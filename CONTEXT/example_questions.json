[
  {
    "domain": 1,
    "scenario": "You are building a code-review agent that must analyze a large monorepo. The agent decomposes work into per-package sub-agents that run in parallel. One sub-agent encounters a syntax error in a config file and crashes.",
    "question": "What is the best recovery strategy for the orchestrator?",
    "options": {
      "A": "Restart the entire pipeline from scratch",
      "B": "Log the failure, mark that package as failed, and continue with remaining sub-agents",
      "C": "Silently ignore the error and report success",
      "D": "Immediately escalate to a human operator and halt all sub-agents"
    },
    "answer": "B",
    "explanation": "Graceful degradation: the orchestrator should isolate the failure to the affected sub-agent, log it for later review, and let the remaining sub-agents complete. This follows the minimal-blast-radius principle in agentic architectures."
  },
  {
    "domain": 2,
    "scenario": "Your team uses Claude Code across multiple repositories. Each repo has its own CLAUDE.md and developers also have personal ~/.claude/CLAUDE.md files.",
    "question": "When a directive in the user-level CLAUDE.md conflicts with the project-level CLAUDE.md, which takes precedence?",
    "options": {
      "A": "User-level always wins",
      "B": "Project-level always wins",
      "C": "The most recently modified file wins",
      "D": "Project-level CLAUDE.md takes precedence; user-level provides defaults"
    },
    "answer": "D",
    "explanation": "In the CLAUDE.md hierarchy, project-level files take precedence over user-level files. User-level CLAUDE.md provides defaults that can be overridden by project-specific settings."
  },
  {
    "domain": 3,
    "scenario": "You need Claude to extract structured product data from unstructured customer reviews and return valid JSON every time.",
    "question": "Which technique most reliably ensures well-formed JSON output?",
    "options": {
      "A": "Ask politely in the system prompt to return JSON",
      "B": "Use prefilling to start the assistant response with '{' and enforce a JSON schema",
      "C": "Use a high temperature to increase creativity",
      "D": "Post-process the output with regex to find JSON-like strings"
    },
    "answer": "B",
    "explanation": "Prefilling the assistant turn with '{' anchors the model into JSON mode. Combining this with a JSON schema definition provides programmatic enforcement of the output structure."
  },
  {
    "domain": 4,
    "scenario": "You are designing an MCP server that provides database query tools to Claude. The server will be used by multiple clients concurrently.",
    "question": "Which transport should you choose for a production multi-client deployment?",
    "options": {
      "A": "stdio — it is the simplest and most reliable",
      "B": "SSE (Server-Sent Events) or streamable HTTP — they support multiple concurrent clients",
      "C": "WebSocket — it has the lowest latency",
      "D": "Raw TCP sockets for maximum control"
    },
    "answer": "B",
    "explanation": "stdio is 1:1 (one client per process). For multi-client production deployments, SSE or streamable HTTP transport is the correct choice as it supports concurrent connections and is the standard remote MCP transport."
  },
  {
    "domain": 5,
    "scenario": "Your agentic application has been running a long multi-step task. The conversation is approaching the context window limit and important early instructions are at risk of being lost.",
    "question": "What is the recommended CALM strategy to handle this?",
    "options": {
      "A": "Start a brand new conversation and re-send all messages",
      "B": "Apply conversation compaction — summarise completed steps into a dense state block and continue",
      "C": "Increase the model's max token parameter",
      "D": "Split the conversation into multiple parallel threads"
    },
    "answer": "B",
    "explanation": "The CALM (Context-Aware LLM Management) framework recommends conversation compaction: summarising completed work into a dense state block preserves task continuity while freeing context space for remaining steps."
  },
  {
    "domain": 1,
    "scenario": "You are building an agentic loop that calls Claude's Messages API repeatedly. The agent should continue calling tools until the task is complete, then stop.",
    "question": "What is the correct way to determine when the agentic loop should terminate?",
    "options": {
      "A": "Set a maximum iteration count (e.g., 10 loops) and stop when reached",
      "B": "Parse the assistant's natural language response for phrases like 'I am done'",
      "C": "Check the stop_reason field in the API response — 'end_turn' means done, 'tool_use' means continue",
      "D": "Use a timer and stop after a fixed duration"
    },
    "answer": "C",
    "explanation": "The canonical agentic loop checks stop_reason in the API response. 'tool_use' means Claude wants to call a tool (continue looping), while 'end_turn' means Claude has completed its response. Parsing natural language or using arbitrary iteration caps are anti-patterns."
  },
  {
    "domain": 1,
    "scenario": "A multi-agent research system uses a coordinator agent that delegates tasks to specialized subagents (search, analyze, synthesize). A developer assumes each subagent automatically inherits the coordinator's full conversation context.",
    "question": "What is wrong with this assumption?",
    "options": {
      "A": "Subagents do inherit context automatically — there is no issue",
      "B": "Subagents have isolated context; the coordinator must explicitly pass relevant findings to each subagent",
      "C": "Subagents share context but only within the same API key",
      "D": "Context inheritance depends on the transport protocol used"
    },
    "answer": "B",
    "explanation": "Subagents do NOT automatically inherit the coordinator's context. Each subagent operates with isolated context. The coordinator must explicitly pass relevant information (task description, findings from other subagents) when spawning or communicating with each subagent."
  },
  {
    "domain": 4,
    "scenario": "Your MCP server exposes 22 tools to a Claude-based agent. During testing, you notice Claude frequently selects the wrong tool for a given task.",
    "question": "What is the most likely cause and best fix?",
    "options": {
      "A": "Add a routing classifier that pre-selects the tool before Claude sees them",
      "B": "Reduce the number of tools to 4-5 per agent and improve tool descriptions with input formats, examples, and boundary conditions",
      "C": "Increase the model's temperature to improve tool selection creativity",
      "D": "Rename all tools with numeric prefixes so Claude can select by number"
    },
    "answer": "B",
    "explanation": "Research shows that 4-5 tools per agent is optimal. Having 18+ tools significantly degrades selection reliability. The fix is to reduce tool count and write detailed descriptions that include input formats, examples, edge cases, and boundaries — not to add routing classifiers."
  },
  {
    "domain": 4,
    "scenario": "An MCP tool for querying an inventory database fails due to a temporary network timeout. The error is returned to Claude as a plain string: 'Error: something went wrong'.",
    "question": "How should the error response be improved?",
    "options": {
      "A": "Return a structured error with errorCategory, isRetryable flag, and a human-readable message",
      "B": "Return an empty result set so Claude thinks the query returned no data",
      "C": "Suppress the error and let Claude try a different approach on its own",
      "D": "Include the full stack trace in the error message"
    },
    "answer": "A",
    "explanation": "The CCA-F emphasizes structured error responses with errorCategory (e.g., 'transient', 'validation', 'permission'), an isRetryable flag indicating whether the error is transient or permanent, and a human-readable description. Suppressing errors or returning empty results as success are anti-patterns."
  },
  {
    "domain": 2,
    "scenario": "Your team wants to enforce a coding standard rule that only applies to test files (*.test.tsx) in a Claude Code project, without affecting other file types.",
    "question": "What is the correct way to configure this?",
    "options": {
      "A": "Add the rule to the project-level CLAUDE.md with a comment saying 'only for test files'",
      "B": "Create a rule file in .claude/rules/ with YAML frontmatter containing a glob pattern like **/*.test.tsx",
      "C": "Create a separate CLAUDE.md in every test directory",
      "D": "Use an environment variable to toggle the rule"
    },
    "answer": "B",
    "explanation": "Claude Code supports path-specific rules via .claude/rules/ directory. Files there can include YAML frontmatter with glob patterns (e.g., **/*.test.tsx) for conditional activation, applying rules only when Claude is working with matching files."
  },
  {
    "domain": 2,
    "scenario": "You need to integrate Claude Code into a CI/CD pipeline to perform automated code reviews on every pull request. The pipeline runs non-interactively.",
    "question": "Which Claude Code flags are essential for this integration?",
    "options": {
      "A": "Use --interactive mode with --verbose for detailed output",
      "B": "Use the -p flag for non-interactive mode and --output-format json for structured, parseable output",
      "C": "Use --daemon mode to keep Claude running between reviews",
      "D": "Use --batch flag with --parallel for concurrent reviews"
    },
    "answer": "B",
    "explanation": "The -p flag enables non-interactive (headless) mode suitable for CI/CD pipelines. Combined with --output-format json, it produces structured output that can be parsed by downstream pipeline steps. These are the canonical flags for Claude Code CI/CD integration."
  },
  {
    "domain": 3,
    "scenario": "You are designing a prompt for Claude to classify customer support tickets into categories. Your initial prompt says 'classify the ticket appropriately'. Results are inconsistent.",
    "question": "What is the best way to improve classification reliability?",
    "options": {
      "A": "Increase the temperature to 1.0 for more diverse classifications",
      "B": "Replace vague instructions with explicit categorical criteria and provide 2-4 few-shot examples showing expected classifications",
      "C": "Add 'please try harder' to the system prompt",
      "D": "Use a longer system prompt repeating the instruction multiple times"
    },
    "answer": "B",
    "explanation": "The CCA-F emphasizes that explicit categorical criteria outperform vague guidance. Combined with 2-4 targeted few-shot examples (especially effective for format consistency and ambiguous scenarios), this approach dramatically improves classification reliability."
  },
  {
    "domain": 3,
    "scenario": "You need to process 10,000 product descriptions through Claude for data extraction. Latency is not a concern — results are needed by tomorrow morning.",
    "question": "Which API approach offers the best cost-performance tradeoff?",
    "options": {
      "A": "Send all requests synchronously in a single-threaded loop",
      "B": "Use the Message Batches API for up to 50% cost savings with up to 24-hour processing",
      "C": "Spin up 10,000 parallel API calls simultaneously",
      "D": "Use streaming mode for each request to get partial results faster"
    },
    "answer": "B",
    "explanation": "The Message Batches API offers up to 50% cost savings with a processing window of up to 24 hours. For latency-tolerant workloads like overnight batch processing, this is the optimal choice. It should NOT be used for real-time or blocking operations."
  },
  {
    "domain": 5,
    "scenario": "A customer support agent uses progressive summarization to manage long conversations. After summarization, the agent loses track of the customer's order number and refund amount mentioned earlier in the conversation.",
    "question": "What should be done to prevent this data loss?",
    "options": {
      "A": "Disable summarization entirely and keep all messages",
      "B": "Extract transactional facts (order numbers, amounts, dates) into a persistent 'case facts' block that is preserved outside the summary",
      "C": "Increase the context window size",
      "D": "Ask the customer to repeat their order number after each summarization"
    },
    "answer": "B",
    "explanation": "Progressive summarization risks losing specific transactional details. The solution is to extract critical facts (amounts, dates, order numbers, account IDs) into a persistent 'case facts' block that sits outside the summarized content and is always preserved in context."
  },
  {
    "domain": 5,
    "scenario": "A customer support agent powered by Claude detects negative sentiment in a customer message and automatically escalates to a human agent.",
    "question": "What is wrong with this escalation design?",
    "options": {
      "A": "Nothing — sentiment-based escalation is best practice",
      "B": "Escalation should be triggered by explicit customer requests, policy gaps, or genuine inability to progress — not by sentiment analysis",
      "C": "The agent should use self-reported confidence scores instead of sentiment",
      "D": "Escalation should only happen after 10 failed attempts"
    },
    "answer": "B",
    "explanation": "Sentiment-based escalation is an anti-pattern on the CCA-F exam. Correct escalation triggers are: explicit customer requests to speak with a human, policy gaps the agent cannot resolve, or genuine inability to make progress. Self-reported confidence scores are also an anti-pattern."
  },
  {
    "domain": 1,
    "scenario": "A financial services company uses a Claude-based agent to process refund requests. The business rule is: refunds over $500 require manager approval. A developer implements this rule in the system prompt.",
    "question": "What is the architectural concern with this approach?",
    "options": {
      "A": "The system prompt is the correct place for all business rules",
      "B": "Critical business rules like financial thresholds should be enforced via programmatic hooks (e.g., PostToolUse), not prompt instructions, because prompts are not deterministic",
      "C": "The threshold should be hardcoded in the model's training data",
      "D": "The rule should be in the user message instead of the system prompt"
    },
    "answer": "B",
    "explanation": "Prompt-based enforcement of critical financial/business rules is an anti-pattern. Prompts are probabilistic and can be circumvented. Deterministic business rules should be enforced via programmatic hooks (e.g., PostToolUse hooks) that check conditions and block/allow actions with certainty."
  },
  {
    "domain": 4,
    "scenario": "You want Claude to always call a specific tool in the next response — for example, to force it to use a 'validate_input' tool before processing user data.",
    "question": "Which tool_choice mode should you use?",
    "options": {
      "A": "tool_choice: 'auto' — Claude will figure out the right tool",
      "B": "tool_choice: 'any' — Claude must call some tool but chooses which one",
      "C": "tool_choice with the specific tool name forced — Claude must call that exact tool",
      "D": "Do not set tool_choice — the default behavior handles this"
    },
    "answer": "C",
    "explanation": "tool_choice supports three modes: 'auto' (Claude may return text or call a tool), 'any' (Claude must call a tool but picks which one), and forced selection with a specific tool name (Claude must call that exact tool). To guarantee a particular tool is called, use forced selection."
  },
  {
    "domain": 2,
    "scenario": "A developer wants to create a custom slash command for their Claude Code project that runs in an isolated subagent context to avoid polluting the main conversation.",
    "question": "How should the custom command be configured to achieve this?",
    "options": {
      "A": "Add 'isolated: true' to the command's YAML frontmatter",
      "B": "Set 'context: fork' in the command definition to run in a forked subagent context",
      "C": "Create the command in ~/.claude/commands/ instead of .claude/commands/",
      "D": "Use the --sandbox flag when invoking the command"
    },
    "answer": "B",
    "explanation": "Custom commands/skills in Claude Code support 'context: fork' which runs the command in a forked subagent context, isolating it from the main conversation. This prevents the command's intermediate work from cluttering the primary context."
  },
  {
    "domain": 5,
    "scenario": "A multi-agent research system synthesizes information from multiple subagents. The final report contains claims but does not track which source each claim came from.",
    "question": "What reliability pattern should be applied?",
    "options": {
      "A": "Add a disclaimer that sources are not tracked",
      "B": "Maintain claim-source mappings with URLs, excerpts, and publication dates to preserve attribution through synthesis",
      "C": "Have Claude regenerate the report multiple times and pick the best one",
      "D": "Use a higher-capability model for better accuracy"
    },
    "answer": "B",
    "explanation": "Information provenance is a key reliability pattern. The system should maintain claim-source mappings that include URLs, relevant excerpts, and publication dates. This preserves attribution through the synthesis process and allows downstream verification of claims."
  },
  {
    "domain": 1,
    "scenario": "You are designing a multi-agent system for a legal document review pipeline. The architect proposes using an orchestrator-worker pattern where the orchestrator dynamically plans which subagents to invoke based on document type. Mid-way through processing, the orchestrator realizes a new document category requires a different analysis approach.",
    "question": "What agentic pattern best handles this situation?",
    "options": {
      "A": "Abort the current run and restart with a new hardcoded plan",
      "B": "Use dynamic replanning — the orchestrator adjusts its plan mid-execution based on intermediate results",
      "C": "Queue the unexpected documents for a separate manual review pipeline",
      "D": "Add all possible document categories to the initial plan to avoid replanning"
    },
    "answer": "B",
    "explanation": "Dynamic replanning is a core agentic pattern where the orchestrator can revise its execution plan mid-run based on intermediate results or unexpected inputs. This is more resilient than static plans and avoids the brittleness of trying to enumerate all possibilities upfront."
  },
  {
    "domain": 1,
    "scenario": "A developer is building an agentic system using the Claude Agent SDK. They need to run three independent research tasks in parallel to reduce total latency.",
    "question": "How should parallel execution be achieved in the Claude Agent SDK?",
    "options": {
      "A": "Send three separate API requests sequentially and cache the results",
      "B": "Use multiple Task tool calls in a single response to spawn parallel subagents",
      "C": "Set a 'parallel: true' flag in the API request body",
      "D": "Use the Message Batches API to process all three at once"
    },
    "answer": "B",
    "explanation": "In the Claude Agent SDK and Claude Code, parallel execution is achieved by making multiple Task tool calls in a single response. Each task runs as a parallel subagent. The Message Batches API is for cost-optimized batch processing, not real-time parallel execution."
  },
  {
    "domain": 2,
    "scenario": "A team is onboarding new developers to a Claude Code project. Senior developers have extensive personal ~/.claude/CLAUDE.md configurations. New team members report that Claude Code behaves differently for them compared to senior devs.",
    "question": "What is the most likely cause of the inconsistency?",
    "options": {
      "A": "New developers are using a different version of Claude Code",
      "B": "User-level CLAUDE.md files are not shared across team members — only project-level CLAUDE.md and .claude/rules/ are version-controlled",
      "C": "Claude Code randomly varies behavior between users",
      "D": "The project-level CLAUDE.md is corrupted"
    },
    "answer": "B",
    "explanation": "User-level ~/.claude/CLAUDE.md is personal and not shared via version control. Only project-level .claude/CLAUDE.md and .claude/rules/ files are committed to the repo and shared with all team members. This is a common gotcha — new team members won't receive user-level instructions."
  },
  {
    "domain": 2,
    "scenario": "An architect is deciding when to use Claude Code's Plan Mode versus direct execution for a large refactoring task that involves changes across 50+ files.",
    "question": "When is Plan Mode the appropriate choice?",
    "options": {
      "A": "Plan Mode should always be used regardless of task size",
      "B": "Plan Mode is best for large-scale changes and architectural decisions involving many files; direct execution suits single-file fixes",
      "C": "Plan Mode is only for generating documentation",
      "D": "Plan Mode should be avoided because it consumes more tokens"
    },
    "answer": "B",
    "explanation": "Plan Mode is designed for large-scale changes (45+ file modifications) and architectural decisions where understanding the full scope before acting is critical. Direct execution is appropriate for single-file fixes and simple, well-scoped changes. Choosing the right mode prevents wasted effort and costly mistakes."
  },
  {
    "domain": 3,
    "scenario": "You have built a prompt that asks Claude to review its own generated code for bugs in the same conversation turn. The self-review consistently misses obvious issues.",
    "question": "Why is the self-review underperforming, and how should it be fixed?",
    "options": {
      "A": "Increase the temperature for the review step to get more creative analysis",
      "B": "Same-session self-review is an anti-pattern — use an independent session without the prior reasoning context for unbiased review",
      "C": "Add 'be very critical' to the review prompt",
      "D": "Use a smaller, faster model for the review step"
    },
    "answer": "B",
    "explanation": "Same-session self-review is a known anti-pattern because Claude is biased by its own prior reasoning in the same context. The correct approach is to use an independent session (separate API call) that does not include the original generation context, enabling unbiased review."
  },
  {
    "domain": 3,
    "scenario": "You are using a validation-retry loop where Claude generates JSON output, a validator checks it, and errors are fed back for correction. The loop works well for formatting errors but consistently fails when required fields contain fabricated data.",
    "question": "What explains this behavior?",
    "options": {
      "A": "The validator is misconfigured and should accept fabricated data",
      "B": "Validation-retry loops are effective for format/syntax mismatches but cannot fix missing information — if the source data lacks a field, retries will not produce it",
      "C": "More retry iterations would eventually produce correct data",
      "D": "The temperature should be set to 0 to prevent fabrication"
    },
    "answer": "B",
    "explanation": "Validation-retry loops excel at correcting format mismatches and syntax errors. However, they cannot conjure information that doesn't exist in the source data. When required fields are absent from the input, Claude may fabricate values, and retrying won't fix this. The solution is to handle missing data at the schema level (e.g., making fields optional)."
  },
  {
    "domain": 4,
    "scenario": "Your team shares MCP server configurations across a Claude Code project. The MCP server requires database credentials that differ per developer.",
    "question": "How should credentials be managed in the shared MCP configuration?",
    "options": {
      "A": "Commit the credentials directly in .mcp.json since it's a private repo",
      "B": "Use environment variable expansion in .mcp.json for credentials, so each developer sets their own values locally",
      "C": "Create a separate .mcp.json for each developer and add all to version control",
      "D": "Store credentials in the CLAUDE.md file"
    },
    "answer": "B",
    "explanation": "MCP server configuration in .mcp.json supports environment variable expansion for credentials. This allows the shared configuration to reference variables (e.g., $DB_PASSWORD) that each developer sets in their local environment, keeping secrets out of version control."
  },
  {
    "domain": 4,
    "scenario": "A Claude-based agent uses an MCP tool to query a product database. The query returns zero results for a valid search term. The tool returns an empty array with a 200 status.",
    "question": "How should the tool distinguish this from an access failure?",
    "options": {
      "A": "Return an error for empty results so Claude knows something went wrong",
      "B": "Return empty results silently — Claude will figure out the difference",
      "C": "Return a structured response that clearly distinguishes 'no matching results found' from error states, including the query that was executed",
      "D": "Always return at least one placeholder result to avoid confusion"
    },
    "answer": "C",
    "explanation": "Silently returning empty results is an anti-pattern because Claude cannot distinguish 'no data found' from 'access denied' or 'query failed'. The tool should return a structured response that explicitly states the outcome (e.g., resultCount: 0, status: 'success', query: '...') so Claude can make informed decisions."
  },
  {
    "domain": 1,
    "scenario": "A subagent in a multi-agent research pipeline encounters a permission error when trying to access a restricted API endpoint. It needs to report back to the coordinator.",
    "question": "What information should the subagent return to the coordinator?",
    "options": {
      "A": "A simple 'failed' status flag",
      "B": "Structured context including failure type, attempted query, any partial results obtained, and suggested alternative approaches",
      "C": "The raw API error response string",
      "D": "Nothing — the coordinator should detect the failure via timeout"
    },
    "answer": "B",
    "explanation": "Error propagation in multi-agent systems requires structured context. The subagent should return the failure type (e.g., permission error), what it tried, any partial results it obtained before failure, and suggested alternatives. This gives the coordinator enough information to replan or delegate to another subagent."
  },
  {
    "domain": 5,
    "scenario": "A multi-agent system aggregates research findings from five subagents into a single report. Two subagents return conflicting data about the same topic — one says market growth is 12% and another says 8%.",
    "question": "How should the synthesis agent handle this conflict?",
    "options": {
      "A": "Average the two numbers and report 10%",
      "B": "Pick the number from the subagent that responded first",
      "C": "Annotate both data points with their sources and flag the conflict for the reader, preserving information provenance",
      "D": "Discard both data points since they conflict"
    },
    "answer": "C",
    "explanation": "When synthesizing conflicting data, the system should preserve information provenance by annotating each claim with its source. Averaging, arbitrary selection, or discarding data all lose information. Flagging conflicts with sources allows downstream consumers to evaluate the discrepancy."
  },
  {
    "domain": 5,
    "scenario": "You are aggregating outputs from multiple subagents into a single context window for final synthesis. The aggregated content is large and the most critical summary was placed in the middle of the input.",
    "question": "What is the risk with this arrangement?",
    "options": {
      "A": "There is no risk — Claude processes all parts of the context equally",
      "B": "The 'lost in the middle' effect means information placed in the middle of large inputs may receive less attention; place key summaries at the beginning",
      "C": "Only the last 1000 tokens are processed",
      "D": "Middle content is automatically compressed by the API"
    },
    "answer": "B",
    "explanation": "The 'lost in the middle' effect is a known phenomenon where LLMs pay less attention to information in the middle of large inputs compared to the beginning and end. The mitigation is to place the most critical summaries and instructions at the beginning of aggregated inputs."
  },
  {
    "domain": 2,
    "scenario": "A team wants to restrict which tools a custom slash command can use in their Claude Code project. The command should only be able to read files and search, not edit or execute code.",
    "question": "How should this restriction be configured?",
    "options": {
      "A": "Add instructions in the command prompt saying 'do not use Edit or Bash tools'",
      "B": "Use the 'allowed-tools' restriction in the command's configuration to whitelist only Read, Grep, and Glob",
      "C": "Create a separate Claude Code installation with limited tool access",
      "D": "Set a global environment variable to disable certain tools"
    },
    "answer": "B",
    "explanation": "Custom slash commands in Claude Code support 'allowed-tools' restrictions that programmatically whitelist which tools the command can access. This is a deterministic enforcement mechanism — more reliable than prompt-based instructions which Claude might not follow."
  },
  {
    "domain": 3,
    "scenario": "You are building a data extraction pipeline that processes invoices. The system needs to extract structured data and you want to guarantee the output conforms to a specific schema with required fields like invoice_number, date, and line_items.",
    "question": "What is the most reliable way to enforce schema conformance?",
    "options": {
      "A": "Describe the schema in natural language in the system prompt",
      "B": "Use tool_use with a JSON schema definition — this eliminates syntax errors and enforces field presence",
      "C": "Parse the output with regex and retry if it doesn't match",
      "D": "Use XML tags to wrap each field in the prompt"
    },
    "answer": "B",
    "explanation": "Using tool_use with a JSON schema definition is the most reliable approach for structured output. It eliminates JSON syntax errors and enforces required fields at the API level. Note that it ensures structural conformance but not semantic correctness — the values themselves may still need validation."
  },
  {
    "domain": 1,
    "scenario": "An e-commerce company runs a Claude-based order processing agent. During peak holiday traffic, the agent must handle 10x normal volume. The architect is considering scaling strategies.",
    "question": "Which scaling pattern is most appropriate for the agentic system?",
    "options": {
      "A": "Increase the context window size to process more orders per conversation",
      "B": "Use horizontal scaling with multiple independent agent instances, each handling separate orders with isolated sessions",
      "C": "Use a single agent with a longer timeout to process all orders sequentially",
      "D": "Switch to a faster model to process orders more quickly"
    },
    "answer": "B",
    "explanation": "Horizontal scaling with independent agent instances is the correct pattern for handling increased load. Each order gets its own isolated session, preventing cross-contamination and enabling true parallelism. Context window size and model speed don't address throughput scaling."
  },
  {
    "domain": 1,
    "scenario": "An architect is designing an agentic system that processes customer onboarding requests. Some steps (identity verification, credit check) must complete before others (account creation, welcome email). Other steps like compliance logging can happen independently.",
    "question": "Which task decomposition strategy should the architect use?",
    "options": {
      "A": "Run all steps in parallel and sort the results afterward",
      "B": "Use prompt chaining for the sequential dependencies and parallel subagents for the independent steps",
      "C": "Run all steps sequentially to avoid complexity",
      "D": "Let Claude decide the execution order dynamically at runtime with no constraints"
    },
    "answer": "B",
    "explanation": "Prompt chaining enforces sequential dependencies (identity verification → credit check → account creation), while independent steps like compliance logging can run as parallel subagents. This hybrid approach balances correctness (sequential where needed) with performance (parallel where possible)."
  },
  {
    "domain": 1,
    "scenario": "A Claude-based agent processes API responses that return timestamps in mixed formats (Unix epoch, ISO 8601, locale strings). Downstream tools expect ISO 8601 only. A developer adds format-conversion instructions to the system prompt.",
    "question": "What is the better architectural approach?",
    "options": {
      "A": "The system prompt approach is correct — Claude handles format conversion well",
      "B": "Use a PostToolUse hook to programmatically normalize timestamps before Claude processes the data",
      "C": "Ask the API provider to change their response format",
      "D": "Add few-shot examples showing each timestamp format conversion"
    },
    "answer": "B",
    "explanation": "PostToolUse hooks intercept tool results before the model processes them, enabling deterministic data transformations like timestamp normalization. Relying on the system prompt for data transformation is unreliable because it's probabilistic. Hooks provide guaranteed, consistent formatting."
  },
  {
    "domain": 1,
    "scenario": "A multi-agent coding assistant allows subagents to install npm packages and modify CI configuration files without user confirmation. During a routine task, a subagent accidentally removes a critical build step from the CI pipeline.",
    "question": "What design principle was violated?",
    "options": {
      "A": "The subagent should have used a faster model",
      "B": "Human-in-the-loop approval should be required for high-impact actions like CI/CD modifications and package installations",
      "C": "The subagent should have run the CI pipeline first to test changes",
      "D": "All subagent actions should be logged to a database"
    },
    "answer": "B",
    "explanation": "Human-in-the-loop interrupt patterns are essential for high-impact, hard-to-reverse actions. Autonomous permission escalation — allowing agents to modify shared infrastructure (CI/CD, dependencies) without user approval — is an anti-pattern. The system should require explicit confirmation for destructive or externally-visible operations."
  },
  {
    "domain": 2,
    "scenario": "A developer notices two configuration files in their Claude Code project: .claude/settings.json and .claude/settings.local.json. They want to add a tool permission that only applies to their local development but should not affect other team members.",
    "question": "Which file should they modify?",
    "options": {
      "A": ".claude/settings.json — it's the standard configuration file",
      "B": ".claude/settings.local.json — it's for personal settings and must not be committed to version control",
      "C": "Either file works — they have identical behavior",
      "D": "~/.claude/settings.json — all personal settings belong at user level"
    },
    "answer": "B",
    "explanation": "settings.local.json is for personal, machine-specific settings and must not be committed to version control. settings.json is the shared project-level configuration that applies consistently across all contributors. Local tool permissions that shouldn't affect teammates belong in settings.local.json."
  },
  {
    "domain": 3,
    "scenario": "You are building a customer-facing chatbot and want to prevent prompt injection attacks where malicious user input attempts to override system instructions or extract sensitive information.",
    "question": "Which defense strategy is most effective?",
    "options": {
      "A": "Tell Claude in the system prompt to ignore any instructions in user messages",
      "B": "Use input validation to filter keywords like 'ignore previous instructions'",
      "C": "Separate system instructions from user input using XML tags, validate and sanitize user input at the application boundary, and limit tool permissions to minimum required scope",
      "D": "Use a lower temperature to make Claude less suggestible"
    },
    "answer": "C",
    "explanation": "Defense-in-depth against prompt injection combines multiple layers: structural separation of system vs. user content (XML tags), input sanitization at the application boundary, and principle of least privilege for tool access. Single-layer defenses like keyword filtering or prompt-based instructions are easily bypassed."
  },
  {
    "domain": 3,
    "scenario": "You want Claude to solve a complex multi-step reasoning problem — analyzing a business scenario, identifying risks, and recommending mitigations. Initial results show shallow analysis with missed edge cases.",
    "question": "Which technique would most improve the depth and quality of reasoning?",
    "options": {
      "A": "Increase max_tokens to allow longer responses",
      "B": "Enable extended thinking to give Claude a dedicated reasoning space before generating its final answer",
      "C": "Add 'think deeply' to the system prompt",
      "D": "Use a higher temperature for more creative analysis"
    },
    "answer": "B",
    "explanation": "Extended thinking provides Claude with a dedicated internal reasoning space (thinking tokens) before producing the final response. This enables deeper multi-step analysis, better edge case identification, and more thorough reasoning — especially valuable for complex architectural and analytical tasks."
  },
  {
    "domain": 4,
    "scenario": "Your team is building an MCP server that will be deployed behind a cloud load balancer and accessed by multiple remote Claude Code instances. You need to choose the transport protocol.",
    "question": "Which transport is recommended for this deployment?",
    "options": {
      "A": "stdio — it's the simplest and works everywhere",
      "B": "SSE (Server-Sent Events) — it's the standard remote transport",
      "C": "Streamable HTTP — it's the recommended remote transport, supporting stateless load balancing and session management via Mcp-Session-Id headers",
      "D": "Raw WebSocket connections for bidirectional streaming"
    },
    "answer": "C",
    "explanation": "Streamable HTTP is the current recommended transport for remote MCP servers. It uses a single HTTP endpoint supporting both request-response and streaming, works behind load balancers (stateless-friendly), includes built-in session management via Mcp-Session-Id headers, and supports OAuth 2.1 authentication. SSE is deprecated for new servers; stdio is local-only."
  },
  {
    "domain": 4,
    "scenario": "An MCP server exposes both tools and resources. A developer is unsure when to use MCP resources versus MCP tools for providing data to Claude.",
    "question": "What is the correct distinction between MCP resources and MCP tools?",
    "options": {
      "A": "Resources and tools are interchangeable — use either for any purpose",
      "B": "Resources expose read-only content catalogs (files, data) that Claude can browse without side effects; tools perform actions or computations that may have side effects",
      "C": "Resources are for authentication and tools are for data access",
      "D": "Resources are faster than tools and should be used whenever possible"
    },
    "answer": "B",
    "explanation": "MCP defines three primitives: resources (read-only content like files and data catalogs), tools (actions/computations with potential side effects), and prompts (reusable prompt templates). Resources reduce unnecessary exploratory tool calls by letting Claude browse available content without executing actions."
  },
  {
    "domain": 5,
    "scenario": "A Claude-based agent performs extended codebase exploration across a large monorepo. After 30+ tool calls, the agent starts referencing 'typical patterns' instead of specific classes and functions it discovered earlier in the session.",
    "question": "What is causing this degradation and how should it be addressed?",
    "options": {
      "A": "The model is running out of memory — restart with a larger instance",
      "B": "Context degradation from a saturated context window; use the scratchpad pattern to persist key findings in an external file and delegate verbose exploration to subagents",
      "C": "The model needs a higher temperature to maintain creativity",
      "D": "This is expected behavior — models cannot maintain accuracy over long sessions"
    },
    "answer": "B",
    "explanation": "Context degradation occurs when the context window fills with verbose tool outputs, causing the model to lose track of earlier specific findings. The scratchpad pattern — writing key discoveries to a persistent file — preserves facts across context boundaries. Delegating verbose exploration to subagents keeps the coordinator's context lean."
  },
  {
    "domain": 5,
    "scenario": "A data extraction pipeline processes invoices from 12 different vendor formats. The team reports overall accuracy of 94%. However, one vendor's invoices consistently fail, producing only 60% accuracy.",
    "question": "What is the reliability concern with the team's evaluation approach?",
    "options": {
      "A": "94% accuracy is acceptable — no changes needed",
      "B": "Aggregate accuracy metrics mask per-category failures; the team should track accuracy by document type and route low-confidence extractions to human review",
      "C": "The team should retrain the model on the failing vendor's format",
      "D": "The failing vendor should be removed from the pipeline to improve the overall metric"
    },
    "answer": "B",
    "explanation": "Aggregate accuracy metrics are an anti-pattern because they mask failures in specific categories. The CCA-F emphasizes tracking accuracy by document type (per-vendor in this case) and using field-level confidence scores to route difficult extractions to human review. Removing problematic categories or relying solely on aggregate metrics hides real quality issues."
  },
  {
    "domain": 5,
    "scenario": "Your application makes repeated API calls with the same large system prompt (8,000 tokens) across hundreds of requests. Each request differs only in the user message. You want to reduce latency and cost.",
    "question": "Which optimization should you apply?",
    "options": {
      "A": "Shorten the system prompt to reduce token count",
      "B": "Use prompt caching with cache_control breakpoints to cache the static system prompt across requests",
      "C": "Switch to a smaller model to reduce per-token cost",
      "D": "Batch all requests into a single API call with all user messages concatenated"
    },
    "answer": "B",
    "explanation": "Prompt caching with cache_control breakpoints allows the static system prompt to be cached and reused across multiple requests. This reduces both latency (cached tokens are processed faster) and cost for requests sharing the same prompt prefix. It's the ideal optimization for high-volume workloads with a stable system prompt."
  },
  {
    "domain": 2,
    "scenario": "A team wants to enforce that Claude Code always runs a linter after editing TypeScript files, without relying on Claude to remember this instruction. The enforcement must be deterministic.",
    "question": "What is the correct mechanism to implement this?",
    "options": {
      "A": "Add 'always run the linter after editing' to CLAUDE.md",
      "B": "Define a PostToolUse hook in .claude/settings.json that triggers on Edit tool calls matching *.ts files and runs the linter command",
      "C": "Create a custom skill that runs the linter",
      "D": "Use a PreToolUse hook to lint files before they are edited"
    },
    "answer": "B",
    "explanation": "Hooks are system-triggered, deterministic shell commands tied to lifecycle events. A PostToolUse hook fires after the Edit tool completes and can run a linter automatically. Unlike CLAUDE.md instructions (probabilistic) or skills (agent-invoked, not guaranteed), hooks execute deterministically on every matching tool call. PreToolUse fires before the edit, which is too early for linting."
  },
  {
    "domain": 2,
    "scenario": "An architect needs to decide between implementing a capability as a Claude Code hook versus a custom skill. The capability is fetching the latest deployment status from an internal API when Claude needs it during troubleshooting.",
    "question": "Should this be a hook or a skill?",
    "options": {
      "A": "A hook — because it provides external data",
      "B": "A skill — because Claude should invoke it based on task relevance, using its reasoning to decide when deployment status is needed",
      "C": "A hook — because it should run on every tool call for observability",
      "D": "Neither — this should be hardcoded in the system prompt"
    },
    "answer": "B",
    "explanation": "Skills are agent-driven tools that Claude actively invokes based on task requirements and reasoning. Hooks are system-triggered at lifecycle events regardless of task context. Fetching deployment status is context-dependent (only needed during troubleshooting), so Claude should decide when to invoke it — making it a skill, not a hook."
  },
  {
    "domain": 2,
    "scenario": "A security-conscious team wants to prevent Claude Code from ever executing 'rm -rf /' or 'DROP TABLE' commands, even if instructed to do so in a prompt. The block must be absolute and not bypassable.",
    "question": "How should this safety constraint be enforced?",
    "options": {
      "A": "Add a strong warning in CLAUDE.md saying 'never run destructive commands'",
      "B": "Define a PreToolUse hook that inspects Bash tool arguments and exits with code 2 to block matching commands",
      "C": "Remove Bash from the allowed tools list entirely",
      "D": "Use a PostToolUse hook to undo destructive commands after execution"
    },
    "answer": "B",
    "explanation": "PreToolUse hooks fire before tool execution and can block operations by exiting with code 2. This provides deterministic, absolute prevention — Claude cannot bypass it because hooks run at the system level, invisible to the agent. CLAUDE.md instructions are probabilistic. Removing Bash entirely is too restrictive. PostToolUse is too late for prevention."
  },
  {
    "domain": 1,
    "scenario": "A startup wants to minimize API costs for their customer support agent. Most queries (80%) are simple FAQ lookups, while 20% require complex reasoning about account-specific situations. They currently route all queries to Claude Sonnet.",
    "question": "What architecture pattern would optimize cost without sacrificing quality?",
    "options": {
      "A": "Switch all traffic to Claude Haiku since it's cheapest",
      "B": "Use a model cascade: attempt with Haiku first for simple queries, escalate to Sonnet only when Haiku's response fails validation or indicates low confidence",
      "C": "Use Opus for all queries to minimize retry costs",
      "D": "Randomly distribute queries between Haiku and Sonnet to average out costs"
    },
    "answer": "B",
    "explanation": "The model cascade pattern routes queries to the cheapest capable model first (Haiku for simple FAQs), then escalates to a more capable model (Sonnet) only when needed. This combines Haiku's cost efficiency (80% of queries) with Sonnet's capability for complex cases (20%), optimizing the cost-quality tradeoff."
  },
  {
    "domain": 1,
    "scenario": "An architect is designing a knowledge assistant that needs to answer questions using the company's internal documentation (50,000+ pages). The documents update weekly.",
    "question": "Which architecture pattern is most appropriate?",
    "options": {
      "A": "Include all documents in the system prompt so Claude has full context",
      "B": "Fine-tune Claude on the company's documents",
      "C": "Use RAG (Retrieval-Augmented Generation): chunk documents, generate embeddings, store in a vector database, retrieve relevant chunks at query time, and pass them as context to Claude",
      "D": "Train a separate classification model to answer questions directly"
    },
    "answer": "C",
    "explanation": "RAG is the standard pattern for grounding LLM responses in large, frequently-updated document collections. It chunks documents, generates embeddings, stores them in a vector database, and retrieves relevant chunks at query time. This avoids context window limits (can't fit 50K pages), stays current without retraining, and provides traceable sources."
  },
  {
    "domain": 4,
    "scenario": "A developer is building an MCP integration and needs to understand the architecture. They are confused about the relationship between MCP hosts, clients, and servers.",
    "question": "What is the correct MCP architecture hierarchy?",
    "options": {
      "A": "Servers connect to hosts, which then connect to clients",
      "B": "Hosts (like Claude Code) contain MCP clients that connect to MCP servers; each server exposes tools, resources, and prompts",
      "C": "Clients and servers are the same component with different configurations",
      "D": "MCP uses a peer-to-peer architecture with no hierarchy"
    },
    "answer": "B",
    "explanation": "MCP follows a host → client → server hierarchy. Hosts (e.g., Claude Code, IDE extensions) contain one or more MCP clients. Each client maintains a connection to an MCP server. Servers expose three primitives: tools (actions with side effects), resources (read-only content), and prompts (reusable templates). Communication uses JSON-RPC over the chosen transport."
  },
  {
    "domain": 4,
    "scenario": "An MCP server needs to authenticate requests from Claude Code in a production environment deployed with Streamable HTTP transport. The team wants to use industry-standard authentication.",
    "question": "Which authentication pattern should be used?",
    "options": {
      "A": "Basic HTTP authentication with username and password",
      "B": "API keys passed in query parameters",
      "C": "OAuth 2.1 — the standard authentication protocol supported by Streamable HTTP transport",
      "D": "Custom JWT tokens with a proprietary signing algorithm"
    },
    "answer": "C",
    "explanation": "Streamable HTTP transport has built-in support for OAuth 2.1 authentication, which is the industry-standard protocol for production MCP deployments. It provides proper token management, scoped permissions, and refresh flows. Basic auth and query parameter API keys are insecure for production; custom JWT schemes add unnecessary complexity."
  },
  {
    "domain": 3,
    "scenario": "You are building a multi-turn conversation agent for customer support. After several turns, Claude's responses start drifting from the original task constraints defined in the system prompt.",
    "question": "What is causing this drift and what is the best mitigation?",
    "options": {
      "A": "The system prompt is too short — make it longer",
      "B": "Multi-turn conversations dilute system prompt influence as user messages accumulate; re-inject key constraints at strategic points in the conversation or use prompt caching to ensure system instructions remain prominent",
      "C": "Increase temperature to make Claude more attentive",
      "D": "The system prompt should be moved to the last user message instead"
    },
    "answer": "B",
    "explanation": "In multi-turn conversations, the influence of the system prompt can diminish as the conversation grows longer. Key constraints should be periodically re-injected (e.g., in user-turn prefixes or assistant-turn prefills) to maintain behavioral consistency. Prompt caching ensures system instructions are processed consistently without added latency."
  },
  {
    "domain": 3,
    "scenario": "A developer is using extended thinking for a complex code analysis task. They want to cache the conversation for follow-up questions but notice thinking blocks behave differently with caching.",
    "question": "How do thinking blocks interact with prompt caching?",
    "options": {
      "A": "Thinking blocks cannot be cached under any circumstances",
      "B": "Thinking blocks are cached directly using cache_control like any other content",
      "C": "Thinking blocks cannot have cache_control applied directly, but they are cached alongside other content when they appear in previous assistant turns in the conversation",
      "D": "Thinking blocks are automatically excluded from all cached conversations"
    },
    "answer": "C",
    "explanation": "Thinking blocks cannot have cache_control applied directly to them, but they CAN be cached when they appear in previous assistant turns as part of a multi-turn conversation. When read from cache, they count as input tokens — creating a tradeoff between extended thinking's reasoning benefits and token costs in cached multi-turn conversations."
  },
  {
    "domain": 5,
    "scenario": "A production agent processes thousands of requests per hour. The team needs to monitor token usage to stay within budget. They currently only track output tokens.",
    "question": "What is missing from their token budget management?",
    "options": {
      "A": "Output tokens are the only metric that matters for cost",
      "B": "They should also track input tokens (including cached token reads), thinking tokens from extended thinking, and set per-request token budgets with alerts for anomalous usage spikes",
      "C": "Token counting is not possible with the Claude API",
      "D": "They should switch to a flat-rate pricing plan instead"
    },
    "answer": "B",
    "explanation": "Comprehensive token budget management requires tracking input tokens (including cached reads which are cheaper but still billable), output tokens, and thinking tokens from extended thinking. Per-request budgets and anomaly alerts prevent runaway costs from stuck loops or unexpectedly verbose responses. Tracking only output tokens misses the majority of costs."
  },
  {
    "domain": 1,
    "scenario": "An agentic system follows an observe-think-act-respond cycle. During the 'act' phase, Claude calls a tool that returns an unexpectedly large result (500KB of JSON). This consumes most of the remaining context window.",
    "question": "What is the best way to prevent this from degrading subsequent iterations of the agentic loop?",
    "options": {
      "A": "Increase the context window to the maximum available size",
      "B": "Use a PostToolUse hook to truncate or summarize large tool results before they enter the context, or delegate the processing to a subagent with fresh context",
      "C": "Set the tool to always return a maximum of 100 characters",
      "D": "Skip the observation phase in the next cycle to save context space"
    },
    "answer": "B",
    "explanation": "Large tool results can saturate the context window and degrade subsequent reasoning. Two complementary strategies are: (1) PostToolUse hooks that truncate or summarize oversized results before they enter context, and (2) delegating processing of large results to a subagent that operates with fresh context and returns only a compact summary to the coordinator."
  },
  {
    "domain": 1,
    "scenario": "You are building a customer support agent that handles returns, billing disputes, and account issues. The agent achieves 80%+ first-contact resolution on routine requests, but occasionally encounters a customer threatening legal action over a disputed charge.",
    "question": "What is the correct escalation behavior for the agent in this situation?",
    "options": {
      "A": "Continue handling the dispute using the standard billing resolution flow since the agent has high resolution rates",
      "B": "Immediately disconnect the conversation and create a ticket for the legal department",
      "C": "Recognize the legal-threat trigger, preserve full conversation context, and perform a warm handoff to a human agent with the context summary attached",
      "D": "Offer the customer a full refund to avoid escalation"
    },
    "answer": "C",
    "explanation": "Human escalation is a critical reliability pattern in agentic customer support systems. The agent should recognize situations beyond its authority (legal threats, safety concerns), preserve the full conversation context, and perform a warm handoff — not a cold transfer — so the human agent has complete context. Autonomous resolution of legal matters or blanket refunds are outside the agent's authorized scope."
  },
  {
    "domain": 2,
    "scenario": "A team wants to enforce different coding conventions depending on the subdirectory. Files under src/frontend/ should follow React best practices, while src/backend/ should follow Python conventions. They want Claude Code to automatically apply the right rules without developers specifying them each time.",
    "question": "What is the recommended mechanism to achieve directory-scoped rules?",
    "options": {
      "A": "Create a single CLAUDE.md at the repo root with conditional if/else logic for each directory",
      "B": "Place separate CLAUDE.md files in src/frontend/ and src/backend/ — subdirectory CLAUDE.md files are automatically scoped to that directory and its children",
      "C": "Use environment variables to switch between rule sets at runtime",
      "D": "Create a PostToolUse hook that checks the file path and injects the appropriate rules"
    },
    "answer": "B",
    "explanation": "Claude Code supports hierarchical CLAUDE.md files. A CLAUDE.md placed in a subdirectory is automatically scoped to that directory and its children. This is the idiomatic way to apply directory-specific conventions — the repo root CLAUDE.md provides defaults, and subdirectory CLAUDE.md files override or extend them for their scope."
  },
  {
    "domain": 2,
    "scenario": "Your CI/CD pipeline runs Claude Code in a headless environment to perform automated code reviews on every pull request. The pipeline needs to parse Claude's output programmatically and must ensure each run is isolated with no state leaking between reviews.",
    "question": "Which combination of Claude Code flags achieves both requirements?",
    "options": {
      "A": "Use --verbose and pipe stdout to a JSON parser",
      "B": "Use -p with the review prompt and --output-format json for machine-parseable output; each -p invocation runs in an isolated session by default",
      "C": "Use --batch-mode and --format=structured to enable CI-friendly output",
      "D": "Run Claude Code interactively and capture the terminal output with script"
    },
    "answer": "B",
    "explanation": "The -p flag runs Claude Code non-interactively with a prompt, providing natural session isolation since each invocation starts fresh. The --output-format json flag produces structured JSON output that can be parsed programmatically by CI tools. This combination is the standard pattern for CI/CD integration."
  },
  {
    "domain": 3,
    "scenario": "You need Claude to extract structured data from messy, unstructured legal documents. The documents contain nullable fields (some contracts lack an expiration date), inconsistent formatting, and occasional OCR artifacts. Previous attempts resulted in Claude hallucinating values for missing fields.",
    "question": "What is the most effective strategy to prevent hallucinated values in extracted fields?",
    "options": {
      "A": "Increase temperature to 1.0 so the model explores more options and finds the correct value",
      "B": "Use a JSON schema with explicit nullable field definitions, instruct Claude to output null for absent data rather than guessing, and add a validation retry loop to catch schema violations",
      "C": "Use regex post-processing to strip any values that look suspicious",
      "D": "Limit the output to 50 tokens so there is no room for hallucination"
    },
    "answer": "B",
    "explanation": "For structured data extraction from messy documents, the key defenses against hallucination are: (1) a strict JSON schema that explicitly marks optional fields as nullable, (2) clear instructions telling the model to use null rather than guess when data is absent, and (3) a validation retry loop that catches schema violations and re-prompts. This is the standard pattern for Scenario 6 (Structured Data Extraction) on the CCA-F exam."
  },
  {
    "domain": 4,
    "scenario": "You are building an MCP tool that queries an external database. Sometimes the database is temporarily unavailable due to maintenance windows. Your current error response simply returns {\"error\": \"Database unavailable\"}, but the calling agent keeps retrying indefinitely, wasting tokens and time.",
    "question": "How should the MCP tool's error response be structured to enable intelligent retry behavior?",
    "options": {
      "A": "Return a longer, more descriptive error message explaining the maintenance schedule",
      "B": "Include structured error metadata with an errorCategory (e.g., 'service_unavailable') and an isRetryable boolean flag so the agent can decide whether to retry or fail gracefully",
      "C": "Return an HTTP 503 status code and let the network layer handle retries",
      "D": "Implement exponential backoff inside the tool itself so the caller never sees the error"
    },
    "answer": "B",
    "explanation": "MCP tool error responses should include structured metadata — particularly errorCategory and isRetryable — so the calling agent can make informed decisions. A non-retryable error (like 'invalid_input') should not trigger retries, while a retryable error (like 'service_unavailable') signals the agent to retry with backoff. Hiding retries inside the tool removes the agent's ability to reason about failures and manage its own token budget."
  },
  {
    "domain": 4,
    "scenario": "A developer is writing tool descriptions for an MCP server that provides file system operations. One tool is described as: 'This tool reads files. It can read any file on the system.' The developer notices that Claude sometimes calls this tool when it should use the Grep tool for searching content.",
    "question": "What is the most effective improvement to the tool description?",
    "options": {
      "A": "Add a disclaimer: 'Do not use this tool for searching'",
      "B": "Rename the tool from 'read_file' to 'read_file_not_search' to make the distinction clearer",
      "C": "Write a precise description that specifies the tool's purpose, expected inputs, output format, and when NOT to use it — e.g., 'Reads the full contents of a single file given an absolute path. Returns file content as text. Use Grep instead when searching for patterns across multiple files.'",
      "D": "Remove the tool and force all file access through Grep"
    },
    "answer": "C",
    "explanation": "Tool descriptions are the primary mechanism Claude uses to decide which tool to call. Effective descriptions should clearly state: what the tool does, what inputs it expects, what it returns, and importantly, when to use alternative tools instead. Including negative guidance ('Use X instead when...') helps Claude disambiguate between similar tools. Vague descriptions lead to incorrect tool selection."
  },
  {
    "domain": 1,
    "scenario": "A multi-agent research system has a coordinator that delegates to four specialized subagents: search, analysis, synthesis, and reporting. The coordinator passes the full user query to each subagent. During testing, you notice the synthesis subagent frequently produces summaries that contradict the analysis subagent's findings.",
    "question": "What is the most likely architectural cause and the best fix?",
    "options": {
      "A": "The synthesis subagent's model is too small; upgrade it to a larger model",
      "B": "The coordinator should pass each subagent only the context it needs — the synthesis subagent should receive the analysis subagent's output as its primary input, not the raw user query alone",
      "C": "Add a voting mechanism where all subagents vote on the final summary",
      "D": "Run the synthesis subagent multiple times and pick the best result"
    },
    "answer": "B",
    "explanation": "In multi-agent research systems, context passing between subagents is critical. The synthesis subagent needs the analysis output as its primary input to produce a consistent summary. Passing only the raw user query causes the synthesis agent to independently interpret the data, leading to contradictions. The coordinator's job is to orchestrate the flow of context — each subagent should receive precisely the inputs it needs from prior stages."
  },
  {
    "domain": 5,
    "scenario": "A long-running agentic session has been operating for hours, processing a large codebase. Claude Code triggers automatic context compaction to stay within the context window. After compaction, the agent forgets a critical architectural decision it made earlier and produces contradictory code.",
    "question": "What pattern should be used to preserve critical decisions across compaction boundaries?",
    "options": {
      "A": "Disable automatic compaction and use a model with a larger context window",
      "B": "Write critical decisions and state to persistent storage (e.g., a scratchpad file or CLAUDE.md) so they survive compaction and can be re-read when needed",
      "C": "Repeat all critical decisions in every message to ensure they are always in context",
      "D": "Set the compaction threshold to the maximum possible value to delay it indefinitely"
    },
    "answer": "B",
    "explanation": "The scratchpad pattern is the recommended approach for preserving critical state across context compaction. By writing important decisions, plans, and state to a persistent file (often called a scratchpad), the agent can re-read this information after compaction. This is more reliable than trying to avoid compaction, which only delays the problem. CLAUDE.md files and dedicated scratchpad files serve as durable memory that survives context window management."
  },
  {
    "domain": 3,
    "scenario": "You are using Claude to classify customer emails into categories. You want Claude to output its reasoning process before the final classification to improve accuracy, but you also need the final answer in a machine-parseable format.",
    "question": "Which combination of techniques best achieves both goals?",
    "options": {
      "A": "Use temperature 0 and request JSON output only — reasoning would add unnecessary tokens",
      "B": "Enable extended thinking (thinking blocks) for internal chain-of-thought reasoning, then have Claude output only the structured JSON classification as its visible response",
      "C": "Ask Claude to output reasoning in XML tags followed by JSON, then parse everything with regex",
      "D": "Make two separate API calls: one for reasoning and one for classification"
    },
    "answer": "B",
    "explanation": "Extended thinking (thinking blocks) allows Claude to perform chain-of-thought reasoning internally without polluting the structured output. The thinking happens in dedicated thinking blocks that are separate from the response, so the visible output can be clean, parseable JSON. This gives you both improved accuracy from reasoning and a machine-friendly output format in a single API call."
  },
  {
    "domain": 5,
    "scenario": "Your team is evaluating a Claude-based agent's reliability by running it against a test suite of 50 cases. The agent passes 48 out of 50 cases consistently. The team concludes the agent is production-ready based on these results.",
    "question": "What is the reliability concern with this evaluation approach?",
    "options": {
      "A": "50 test cases is always sufficient for production readiness",
      "B": "The test suite may not cover edge cases, adversarial inputs, or distribution shifts that occur in production; the team should also test with out-of-distribution inputs, monitor live performance, and establish regression baselines",
      "C": "The two failing cases should simply be excluded from the test suite",
      "D": "The agent should be retrained on the failing cases until it achieves 100%"
    },
    "answer": "B",
    "explanation": "A high pass rate on a fixed test suite can create false confidence. Production-readiness evaluation should include: edge cases and adversarial inputs not in the original test set, out-of-distribution inputs that reflect real-world variability, live monitoring and alerting after deployment, and regression baselines to detect degradation over time. A static 96% pass rate does not account for the long tail of production scenarios."
  },
  {
    "domain": 2,
    "scenario": "A developer wants to create a custom slash command in Claude Code that spawns a subagent to research a topic without polluting the main conversation's context window. The command should fork the context so the subagent operates independently.",
    "question": "What is the correct way to configure this behavior in a custom slash command?",
    "options": {
      "A": "Add 'context: fork' to the slash command's frontmatter so it runs in a forked context that does not affect the main conversation",
      "B": "Use the --isolate flag when defining the command in .claude/commands/",
      "C": "Create a separate CLAUDE.md file for the command to read",
      "D": "Manually clear the conversation history before running the command"
    },
    "answer": "A",
    "explanation": "Custom slash commands (skills) support a 'context: fork' frontmatter directive that causes the command to execute in a forked context. This means the subagent operates with a copy of the current context but its actions and results do not pollute the main conversation window. This is the idiomatic way to run exploratory or research tasks without consuming main context."
  },
  {
    "domain": 4,
    "scenario": "A Salesforce architect built an MCP server that runs as a local subprocess on one developer's machine via Claude Code, reading org credentials from environment variables. The team now wants the same server hosted centrally so a dozen developers and a CI runner can all use it concurrently over the network.",
    "question": "What transport and auth change is required?",
    "options": {
      "A": "Keep stdio but share the same environment variables across all machines",
      "B": "Migrate to legacy SSE transport, which is purpose-built for multi-developer access",
      "C": "Migrate from stdio to Streamable HTTP with OAuth 2.1 — stdio is a single-client, local-subprocess transport and cannot serve concurrent remote clients",
      "D": "Run a separate stdio subprocess per developer behind a shared VPN"
    },
    "answer": "C",
    "explanation": "stdio spawns one server subprocess per client locally, with secrets injected via env vars — it cannot serve multiple concurrent remote clients. For centralized multi-client access the server must move to Streamable HTTP (single /mcp endpoint, session management via Mcp-Session-Id, load-balancer friendly) with OAuth 2.1 for authentication. SSE is the deprecated remote transport and should not be chosen for new deployments."
  },
  {
    "domain": 4,
    "scenario": "An MCP server exposes a create_lead tool to a Claude agent that processes inbound web forms. Under retry conditions (network blips, the agent re-calling after a timeout), the same web form sometimes produces two or three duplicate Lead records in Salesforce.",
    "question": "Which tool-design change best prevents the duplicates?",
    "options": {
      "A": "Add a note in the tool description telling Claude not to call create_lead twice",
      "B": "Make the operation idempotent — accept a client-supplied idempotency key (or dedupe on a natural key like email) so repeated calls with the same key return the existing record instead of creating a new one",
      "C": "Lower the agent's temperature so it is less likely to retry",
      "D": "Wrap the tool so it silently ignores the second call"
    },
    "answer": "B",
    "explanation": "Tools that create state should be idempotent wherever possible. Accepting an idempotency key (or deduplicating on a natural unique key) means retries are safe: the same key returns the already-created record rather than producing duplicates. Prompt-based instructions are probabilistic and cannot guarantee the behavior; silently ignoring calls hides information the agent needs."
  },
  {
    "domain": 1,
    "scenario": "A marketing team uses a Claude agent to draft product announcement copy. Single-pass generations are inconsistent: some drafts miss the required call-to-action or exceed the length limit. The team wants higher quality without hand-reviewing every draft.",
    "question": "Which orchestration pattern is most appropriate?",
    "options": {
      "A": "Prompt chaining with more fixed steps",
      "B": "Evaluator-optimizer: one agent generates the draft, a second critiques it against explicit criteria, and the generator revises until the critique passes or a max-iteration cap is hit",
      "C": "Hub-and-spoke with ten parallel drafting workers",
      "D": "Routing: classify the product type then dispatch to a specialized model"
    },
    "answer": "B",
    "explanation": "The evaluator-optimizer pattern fits tasks with clear quality criteria that benefit from iterative refinement: a generator produces output, an evaluator scores it against explicit rules (CTA present, length within limit), and the loop repeats until criteria are met or a max-iteration cap stops it. It pays off precisely when quality is measurable and first drafts are unreliable."
  },
  {
    "domain": 1,
    "scenario": "A shared support inbox receives a mix of password resets, billing questions, and complex integration troubleshooting. Simple categories have canned resolutions; complex ones need deep reasoning and tool access. The team wants each request handled by the right specialized handler.",
    "question": "Which pattern should the architect use first?",
    "options": {
      "A": "Send every request through the most capable handler to be safe",
      "B": "Parallelization — fan every request out to all handlers and merge",
      "C": "Routing — classify the incoming request, then dispatch it to the handler specialized for that category",
      "D": "Evaluator-optimizer on every request"
    },
    "answer": "C",
    "explanation": "Routing classifies an input and dispatches it to the handler best suited for that category, letting simple requests use cheap canned flows and reserving expensive reasoning/tools for complex ones. Sending everything to the top handler wastes cost; fanning out to all handlers is wasteful and produces conflicting outputs. Use the simplest pattern that fits — here, routing."
  },
  {
    "domain": 2,
    "scenario": "A project's root CLAUDE.md has grown to 600 lines: coding standards, a full API reference, onboarding docs, and release procedures. Developers notice every session now starts with less available context and higher cost, and Claude sometimes ignores rules buried deep in the file.",
    "question": "What is the best remediation?",
    "options": {
      "A": "Leave it as-is; more context in CLAUDE.md always improves behavior",
      "B": "Keep CLAUDE.md short with the always-relevant rules and use @path imports (or move reference material to files Claude reads on demand) so detail is pulled in just-in-time rather than loaded every session",
      "C": "Move the entire file to the user-level ~/.claude/CLAUDE.md",
      "D": "Split it into one CLAUDE.md per developer"
    },
    "answer": "B",
    "explanation": "CLAUDE.md is injected into every session and consumes context on each turn, so it should stay lean and hold only consistently-relevant guidance. Bulky reference material belongs in separate files pulled in on demand via @path imports or just-in-time reads. This reduces per-session cost and keeps the high-priority rules prominent instead of buried."
  },
  {
    "domain": 3,
    "scenario": "An email-to-case flow uses Claude to assign a priority (P1/P2/P3/P4) to inbound Salesforce cases. The current prompt says 'set the priority based on urgency'. Results are inconsistent, and the output sometimes contains prose instead of a clean label.",
    "question": "Which combination most improves reliability and parseability?",
    "options": {
      "A": "Raise temperature so the model considers more options",
      "B": "Replace the vague instruction with explicit per-level criteria, give 3-4 XML-tagged few-shot examples covering edge cases, and constrain the output to an enum (P1|P2|P3|P4) via a tool/JSON schema",
      "C": "Repeat 'be accurate' several times in the system prompt",
      "D": "Ask for a long written justification before the label"
    },
    "answer": "B",
    "explanation": "Reliable classification comes from explicit categorical criteria plus 3-4 diverse few-shot examples (ideally covering ambiguous edge cases), with the label space constrained by an enum enforced through a tool/JSON schema. XML tags cleanly separate instructions, examples, and the case text. Temperature and exhortations ('be accurate') do not fix vague criteria or guarantee a parseable label."
  },
  {
    "domain": 3,
    "scenario": "Claude must return only a JSON object for a downstream parser, but it keeps prefacing the JSON with conversational text like 'Sure, here is the data you requested:' which breaks the parser.",
    "question": "What is the simplest reliable fix?",
    "options": {
      "A": "Add 'do not include any preamble' and hope the model complies",
      "B": "Prefill the assistant turn so the response starts with '{', which forces the model directly into the JSON object and skips any preamble",
      "C": "Increase max_tokens so the preamble has room to finish",
      "D": "Strip the preamble afterward with a regex on every response"
    },
    "answer": "B",
    "explanation": "Prefilling the assistant turn with '{' anchors the model into emitting the JSON object immediately, eliminating conversational preamble at the source. It is more reliable than instructing the model to omit preamble (probabilistic) and cleaner than brittle regex post-processing. Pairing the prefill with a stop sequence or schema further hardens the output."
  },
  {
    "domain": 5,
    "scenario": "A Claude agent with write access to Salesforce is asked to 'clean up stale opportunities'. Its plan would mass-delete roughly 4,000 Opportunity records across multiple accounts in a single irreversible operation.",
    "question": "What reliability control should govern this action?",
    "options": {
      "A": "Proceed automatically since the user explicitly asked for cleanup",
      "B": "Lower the agent's temperature before executing the deletion",
      "C": "Require a human-in-the-loop approval step before the destructive bulk operation, surfacing the exact scope (record count, affected accounts) for confirmation",
      "D": "Delete the records but log each deletion for later review"
    },
    "answer": "C",
    "explanation": "Destructive, large-blast-radius, irreversible actions are a canonical human-in-the-loop trigger. The agent should pause and present the precise scope (4,000 records, which accounts) for explicit human approval before executing. A vague instruction like 'clean up' does not authorize mass deletion, and after-the-fact logging does not undo an irreversible operation."
  },
  {
    "domain": 1,
    "scenario": "You are designing a document-processing pipeline that must extract data from PDFs, run sentiment analysis, and generate summaries. Each stage requires logging and validation of intermediate outputs.",
    "question": "Which architectural pattern best supports injecting cross-cutting concerns like logging and validation between processing stages?",
    "options": {
      "A": "A single monolithic prompt that performs all three tasks at once",
      "B": "Pre/post execution hooks that inject logging and validation while controlling execution flow between stages",
      "C": "Three completely independent API calls with no shared state",
      "D": "Hardcoding logging statements inside each prompt's system message"
    },
    "answer": "B",
    "explanation": "Pre/post execution hooks allow cross-cutting concerns (logging, validation) to be injected between processing stages without coupling them to the core logic. This separates concerns cleanly and enables consistent instrumentation across the pipeline."
  },
  {
    "domain": 1,
    "scenario": "A research agent performs a 15-step financial analysis that consumes approximately 180K tokens. At step 8, the agent needs to transition to a new context window because the current one is nearly exhausted.",
    "question": "How should the agent preserve state across this context transition?",
    "options": {
      "A": "Use a memory tool to serialize state into JSON checkpoints, enabling explicit state tracking across context boundaries",
      "B": "Rely on implicit summarization by the model to carry forward all relevant details",
      "C": "Spawn a new agent with only the final conclusion from the first context",
      "D": "Use git-based checkpointing to save intermediate results to disk"
    },
    "answer": "A",
    "explanation": "Explicit state tracking via JSON checkpoints provides clean, auditable transitions across context boundaries. Implicit summarization risks losing critical details, while spawning a new agent with only the final conclusion loses the reasoning chain. Git-based checkpointing adds unnecessary infrastructure complexity."
  },
  {
    "domain": 1,
    "scenario": "A long-running research agent must be able to recover from crashes without replaying side effects (e.g., re-sending emails or re-creating database records) that were already completed before the crash.",
    "question": "What pattern enables safe resumption without re-executing completed side effects?",
    "options": {
      "A": "Restart the agent from the beginning each time",
      "B": "Persist completed tool calls and resume from the last checkpoint, skipping already-executed steps",
      "C": "Disable all tools after the first turn to prevent re-execution",
      "D": "Embed tool results directly in the prompt so the model knows what was already done"
    },
    "answer": "B",
    "explanation": "Durable agents persist completed tool calls to avoid re-executing side effects on recovery. This checkpoint-and-resume pattern ensures idempotent recovery — the agent picks up where it left off without duplicating actions that have real-world consequences."
  },
  {
    "domain": 1,
    "scenario": "You need to process 50 PDF documents, each taking 2-3 seconds to extract data from. Your context window is 200K tokens. You want to maximize throughput while avoiding resource exhaustion.",
    "question": "Which processing strategy best balances throughput and reliability?",
    "options": {
      "A": "Process all 50 sequentially in a single agent with SQLite persistence",
      "B": "Spawn 10 parallel subagents with disk persistence for intermediate results, processing 5 documents each",
      "C": "Launch 50 concurrent agents, one per document",
      "D": "Use a single agent with a tool loop that processes documents one at a time"
    },
    "answer": "B",
    "explanation": "Bounded parallelism (10 subagents) avoids the resource exhaustion risk of 50 concurrent agents while dramatically improving throughput over sequential processing. Disk persistence of intermediate results ensures no work is lost if a subagent fails."
  },
  {
    "domain": 2,
    "scenario": "A developer needs codebase context that is accessible whether they use Claude Code in the CLI, in VS Code, or in another IDE. They also want real-time IDE-specific features like inline suggestions.",
    "question": "What is the recommended approach for cross-IDE persistent context?",
    "options": {
      "A": "Use global, project, and folder-level CLAUDE.md files for persistent context, supplemented by IDE-specific extensions for real-time features",
      "B": "Rely solely on IDE extensions, as they handle all context needs",
      "C": "Use only project-level CLAUDE.md; folder-level and global files are not supported",
      "D": "Store context in environment variables that each IDE reads on startup"
    },
    "answer": "A",
    "explanation": "CLAUDE.md files (at global, project, and folder levels) provide persistent context that works across all interfaces — CLI and IDEs alike. IDE extensions complement this with real-time features like inline suggestions, but the CLAUDE.md hierarchy is the foundation for portable project context."
  },
  {
    "domain": 2,
    "scenario": "Your team wants to define path-specific coding rules — for example, all files under src/api/ must use snake_case function names, while files under src/ui/ must use camelCase. These rules should be version-controlled and automatically applied.",
    "question": "What is the recommended mechanism for path-specific rules in Claude Code?",
    "options": {
      "A": "Add conditional logic to the project-level CLAUDE.md using if/else blocks",
      "B": "Use .claude/rules/ directory with files that have YAML frontmatter specifying glob patterns for path-based applicability",
      "C": "Create separate CLAUDE.md files in every subdirectory with duplicated rules",
      "D": "Define path rules in settings.json under a pathRules key"
    },
    "answer": "B",
    "explanation": "The .claude/rules/ directory supports rule files with YAML frontmatter that specifies glob patterns, enabling precise path-scoped rules. This is the dedicated mechanism for path-specific directives, and it integrates cleanly with version control."
  },
  {
    "domain": 3,
    "scenario": "Your complex code-review task is being cut off because max_tokens is set to 500. You see stop_reason: 'max_tokens' in the API response. You need complete output without wasting tokens on unnecessarily high limits.",
    "question": "What is the best strategy to resolve the truncation while controlling costs?",
    "options": {
      "A": "Incrementally increase max_tokens, monitor stop_reason in each response, and break the task into multiple calls if needed",
      "B": "Remove the max_tokens limit entirely to ensure the response is never truncated",
      "C": "Set max_tokens equal to the full context window size",
      "D": "Ignore stop_reason since streaming mode handles truncation automatically"
    },
    "answer": "A",
    "explanation": "Incrementally adjusting max_tokens while monitoring stop_reason provides a controlled approach to finding the right balance between completeness and cost. Removing the limit entirely wastes tokens, and setting it to the full context window is excessive. The stop_reason field is essential for detecting truncation regardless of streaming mode."
  },
  {
    "domain": 3,
    "scenario": "You want Claude to perform deep analysis of a complex architectural decision before producing its final recommendation. You set max_thinking_tokens=5000 in the API request.",
    "question": "How does the max_thinking_tokens parameter affect Claude's behavior?",
    "options": {
      "A": "It forces Claude to use exactly 5000 tokens for thinking, no more and no less",
      "B": "It enables adaptive thinking only, ignoring extended thinking",
      "C": "The request will be rejected because max_thinking_tokens is not a valid parameter",
      "D": "It enables extended thinking with a budget of up to 5000 tokens, allowing the model to allocate flexibly within that limit"
    },
    "answer": "D",
    "explanation": "The max_thinking_tokens parameter sets a maximum budget for extended thinking. The model can use fewer tokens if the task doesn't require the full budget, providing flexibility while preventing runaway thinking costs."
  },
  {
    "domain": 4,
    "scenario": "Your MCP server receives JSON-RPC requests from clients but never sends responses back. Clients time out waiting. The server logs show requests are being received and parsed correctly.",
    "question": "What is the most likely cause of this issue?",
    "options": {
      "A": "The server's event loop is blocked, preventing response processing and sending",
      "B": "TCP keep-alive settings are misconfigured",
      "C": "JSON-RPC response IDs are not matching request IDs",
      "D": "Buffer flow control is preventing data from being flushed"
    },
    "answer": "A",
    "explanation": "A blocked event loop is the most common cause of an MCP server that receives requests but never responds. If the event loop is blocked by synchronous operations, the server cannot process the response and send it back, even though requests are being received. This is a critical debugging priority for async MCP servers."
  },
  {
    "domain": 4,
    "scenario": "Your MCP server publishes resource_updated notifications to all connected clients whenever any resource changes. As client count grows, notification traffic is causing performance issues.",
    "question": "What is the recommended approach to reduce unnecessary notification traffic?",
    "options": {
      "A": "This is expected MCP behavior and clients should handle the volume",
      "B": "Implement subscription-based filtering so clients only receive notifications for resources they have subscribed to",
      "C": "Add client-side filtering to discard irrelevant notifications after receipt",
      "D": "This is a protocol violation — servers should never send unsolicited notifications"
    },
    "answer": "B",
    "explanation": "Subscription-based notification filtering is the MCP best practice for reducing traffic. Rather than broadcasting every change to every client, the server should track which resources each client has subscribed to and only send relevant notifications. Client-side filtering still wastes bandwidth."
  },
  {
    "domain": 4,
    "scenario": "Your MCP server serves database query results that are 10+ MB and are requested multiple times within 5-minute windows by different clients. The underlying queries are expensive (3-5 seconds each).",
    "question": "Which caching strategy is most appropriate for this MCP resource?",
    "options": {
      "A": "Implement server-side caching with a TTL matching the expected freshness window",
      "B": "Send cache hints to clients and let each client manage its own cache",
      "C": "Use streaming responses to reduce perceived latency instead of caching",
      "D": "Implement client-side deduplication to avoid redundant requests"
    },
    "answer": "A",
    "explanation": "Server-side caching with TTL is the most efficient approach for expensive, frequently-accessed MCP resources. It avoids re-executing costly queries, serves all clients from the same cache, and the TTL ensures freshness. Client-side caching requires each client to independently manage state and doesn't prevent duplicate expensive queries."
  },
  {
    "domain": 5,
    "scenario": "Your evaluation pipeline for a Claude-based agent uses a single LLM judge to score outputs on a 1-5 scale. The team notices that scores are inconsistent — the same output receives different scores when evaluated multiple times.",
    "question": "What is the most effective improvement to evaluation reliability?",
    "options": {
      "A": "Increase the judge model's temperature to explore more scoring possibilities",
      "B": "Use multiple independent judge evaluations and aggregate scores, combined with clear rubric criteria and few-shot scoring examples",
      "C": "Switch to a smaller, faster model for judging to reduce variance",
      "D": "Cache the first score for each output and always return the cached result"
    },
    "answer": "B",
    "explanation": "Evaluation reliability improves through multiple independent assessments (reducing variance via aggregation), explicit rubric criteria (reducing ambiguity), and few-shot examples (calibrating the judge). Caching scores masks the reliability problem rather than solving it, and increasing temperature would increase variance."
  },
  {
    "domain": 5,
    "scenario": "A customer's production agent confidently provides incorrect information to end users. The team asks you to recommend an architectural fix to reduce hallucination risk in the agent's responses.",
    "question": "Which architectural approach most effectively reduces confident hallucination in a production agent?",
    "options": {
      "A": "Lower the temperature to 0 to eliminate all randomness",
      "B": "Add a disclaimer to every response stating the information may be inaccurate",
      "C": "Implement RAG with verified knowledge sources and add a verification step where the agent cross-checks claims against retrieved documents before responding",
      "D": "Increase max_tokens so the agent has more room to self-correct"
    },
    "answer": "C",
    "explanation": "RAG with verified sources grounds the agent's responses in factual data, and a verification step adds a systematic check against hallucination. Temperature=0 does not eliminate hallucination (it only makes outputs more deterministic), disclaimers don't fix the root cause, and more tokens don't inherently improve accuracy."
  },
  {
    "domain": 1,
    "scenario": "You are designing a content moderation pipeline that must classify user-generated posts across 12 categories. Each post needs a category label, a confidence score, and an optional human-review flag. The system processes 50,000 posts per day. Accuracy matters, but cost is a concern.",
    "question": "Which model selection strategy best balances cost and accuracy for this pipeline?",
    "options": {
      "A": "Use Opus for every post to maximize accuracy across all categories",
      "B": "Use Haiku for initial classification, then route low-confidence results to Sonnet for re-evaluation",
      "C": "Use Sonnet for all posts as a balanced middle ground",
      "D": "Randomly distribute posts across all three models to average out costs"
    },
    "answer": "B",
    "explanation": "A model cascade (Haiku → Sonnet) optimizes cost by handling easy classifications cheaply with Haiku while escalating ambiguous cases to Sonnet. At 50K posts/day, using Opus for everything is cost-prohibitive. Random distribution wastes Opus capacity on easy cases. Sonnet-for-all is better than Opus-for-all but still more expensive than a cascade for the majority of straightforward classifications."
  },
  {
    "domain": 1,
    "scenario": "An architect is choosing between two designs for a document analysis system. Design A uses a single detailed mega-prompt that instructs Claude to extract entities, summarize, classify, and generate a report in one call. Design B uses prompt chaining: extract → summarize → classify → report, with each step as a separate API call.",
    "question": "What is the primary architectural advantage of Design B (prompt chaining) over Design A (mega-prompt)?",
    "options": {
      "A": "Prompt chaining is always faster because it uses fewer tokens total",
      "B": "Each step can be independently validated, retried, and debugged, and intermediate results can be inspected",
      "C": "Prompt chaining eliminates the need for system prompts entirely",
      "D": "A single mega-prompt cannot handle multiple tasks regardless of model capability"
    },
    "answer": "B",
    "explanation": "Prompt chaining decomposes complex tasks into discrete steps, enabling independent validation, retry on failure, and easier debugging. Mega-prompts can handle multiple tasks but make it harder to isolate failures or inspect intermediate results. Chaining typically uses more tokens (not fewer), and system prompts are still used in chained calls."
  },
  {
    "domain": 2,
    "scenario": "A team wants to enforce that Claude Code always runs 'npm test' after modifying any .ts file in the src/ directory. They want this to happen automatically without requiring manual invocation.",
    "question": "Which Claude Code mechanism is the correct way to implement this automatic behavior?",
    "options": {
      "A": "Add a custom slash command that runs npm test",
      "B": "Configure a PostToolUse hook that triggers on file edits matching src/**/*.ts",
      "C": "Add an instruction in CLAUDE.md saying 'always run npm test after editing TypeScript files'",
      "D": "Create a .claude/rules/testing.md file with test instructions"
    },
    "answer": "B",
    "explanation": "PostToolUse hooks execute shell commands automatically in response to tool invocations (like file edits), and can be scoped to specific file patterns. Slash commands require manual invocation. CLAUDE.md instructions are advisory and not enforced programmatically. Rules files provide context but don't trigger automatic actions."
  },
  {
    "domain": 2,
    "scenario": "A DevOps engineer wants to use Claude Code in a GitHub Actions workflow to automatically review PRs. The workflow should run non-interactively, output structured JSON results, and never prompt for user input.",
    "question": "Which combination of Claude Code capabilities enables this CI/CD integration?",
    "options": {
      "A": "Use the --interactive flag with a custom CLAUDE.md that says 'do not ask questions'",
      "B": "Use the -p flag for non-interactive mode with --output-format json for structured output",
      "C": "Pipe the prompt via stdin and parse stdout text manually",
      "D": "Run Claude Code in Plan Mode with --auto-approve to skip all confirmations"
    },
    "answer": "B",
    "explanation": "The -p (print) flag runs Claude Code non-interactively with a single prompt, and --output-format json produces structured JSON output suitable for CI/CD parsing. Plan Mode is for interactive planning, not CI automation. While stdin piping works, the -p flag with JSON output is the recommended and more reliable approach for headless CI/CD usage."
  },
  {
    "domain": 3,
    "scenario": "A developer is building a data extraction pipeline that pulls structured records from legal contracts. Some fields (like 'arbitration_clause' and 'penalty_amount') are not present in every contract. The current schema requires all fields, causing Claude to hallucinate values for missing fields.",
    "question": "What is the most effective schema design change to prevent hallucinated values for optional fields?",
    "options": {
      "A": "Add a system prompt instruction saying 'leave fields blank if not found'",
      "B": "Make optional fields nullable in the JSON schema and instruct the model to return null when information is absent",
      "C": "Remove optional fields from the schema entirely",
      "D": "Add a post-processing step that deletes any field with suspicious values"
    },
    "answer": "B",
    "explanation": "Making fields nullable in the JSON schema gives Claude a valid way to express 'not found' without hallucinating. This is a well-established pattern for data extraction: nullable fields with explicit null values prevent the model from inventing data to satisfy required schema constraints. Removing fields loses the ability to extract them when present. Post-processing is reactive and unreliable."
  },
  {
    "domain": 3,
    "scenario": "You need Claude to generate a response that starts with a specific XML tag structure for downstream parsing. The response must begin with '<analysis>' without any preamble text.",
    "question": "Which prompting technique most reliably ensures the response starts with the exact required format?",
    "options": {
      "A": "Add 'IMPORTANT: Your response must start with <analysis>' to the system prompt",
      "B": "Use assistant-turn prefilling by setting the beginning of the assistant message to '<analysis>'",
      "C": "Provide five few-shot examples that all start with <analysis>",
      "D": "Set a stop sequence of '</analysis>' to constrain the output"
    },
    "answer": "B",
    "explanation": "Assistant-turn prefilling places text at the start of Claude's response, guaranteeing the output begins with the specified format. This is the most reliable technique for controlling response opening. System prompt instructions can be ignored, few-shot examples suggest but don't guarantee format, and stop sequences control where output ends, not where it begins."
  },
  {
    "domain": 4,
    "scenario": "A team is building an MCP server that provides both real-time stock prices (requiring API calls) and a company directory (a static dataset loaded at startup). They are deciding how to expose these through MCP.",
    "question": "What is the correct way to model these two capabilities using MCP primitives?",
    "options": {
      "A": "Expose both as MCP tools since they both return data",
      "B": "Expose the company directory as an MCP resource (read-only data) and stock prices as an MCP tool (requires action/computation)",
      "C": "Expose both as MCP resources since they both provide information",
      "D": "Expose the company directory as an MCP prompt template and stock prices as a tool"
    },
    "answer": "B",
    "explanation": "MCP distinguishes between resources (static or semi-static data the model can read) and tools (operations that perform actions or computations). The company directory is a natural resource—pre-loaded, read-only data. Stock prices require an API call each time, making them a tool. Exposing everything as tools wastes the resource abstraction; exposing API calls as resources misrepresents their nature."
  },
  {
    "domain": 4,
    "scenario": "An MCP tool named 'update_config' accepts a JSON object parameter but frequently receives malformed input from the model. The tool description says: 'Updates configuration settings.'",
    "question": "What is the most likely cause of the malformed input, and what is the best fix?",
    "options": {
      "A": "The model is too small; switch to Opus for all tool calls",
      "B": "The tool description is too vague—add explicit parameter descriptions with examples and constraints in the JSON schema",
      "C": "Add client-side validation to reject and re-prompt on every malformed call",
      "D": "Switch from JSON parameters to plain-text string parameters to avoid formatting issues"
    },
    "answer": "B",
    "explanation": "Vague tool descriptions are the primary cause of malformed tool inputs. Adding explicit parameter descriptions, enum constraints, examples, and clear JSON schema definitions helps the model construct correct inputs. Client-side validation is a useful safety net but doesn't address the root cause. Plain-text parameters lose structure and create worse problems."
  },
  {
    "domain": 5,
    "scenario": "A long-running coding agent session is approaching the context window limit. The agent has accumulated extensive file exploration results, but also has critical architectural decisions and a list of remaining tasks that must not be lost.",
    "question": "What is the recommended strategy to handle this context pressure?",
    "options": {
      "A": "Restart the session and re-read all files from scratch",
      "B": "Write critical decisions and remaining tasks to a scratchpad file before compaction occurs, so they persist across context boundaries",
      "C": "Increase max_tokens to expand the context window",
      "D": "Delete older messages from the conversation history manually"
    },
    "answer": "B",
    "explanation": "The scratchpad pattern—writing critical state (decisions, task lists, key findings) to a file—ensures information survives compaction. When the context is compressed, the agent can re-read the scratchpad to restore essential state. Restarting loses all progress. max_tokens controls output length, not context window size. Manual message deletion is not a standard API feature."
  },
  {
    "domain": 5,
    "scenario": "A production chatbot uses prompt caching to reduce costs on repeated system prompt and few-shot example blocks. The team notices that cache hit rates drop significantly whenever they enable extended thinking.",
    "question": "What explains the drop in cache hit rates when extended thinking is enabled?",
    "options": {
      "A": "Extended thinking disables prompt caching entirely as a safety measure",
      "B": "Thinking blocks introduce variable-length content before the cached turn, which can invalidate the cache prefix match",
      "C": "Prompt caching only works with Haiku, and extended thinking requires Sonnet or Opus",
      "D": "The thinking tokens consume the cache storage quota, evicting the cached prompts"
    },
    "answer": "B",
    "explanation": "Prompt caching relies on prefix matching—the cached content must appear as an exact prefix of the new request. Extended thinking inserts variable-length thinking blocks into the conversation, which can shift content positions and break the prefix match required for cache hits. Prompt caching is not disabled by thinking, works across model tiers, and does not have a shared storage quota with thinking tokens."
  },
  {
    "domain": 1,
    "scenario": "An architect is designing a customer support agent that handles refund requests. Most requests are straightforward (standard policy), but some involve exceptions requiring manager approval. The current design has the agent attempt to handle all requests autonomously.",
    "question": "What is the correct escalation pattern for this agent?",
    "options": {
      "A": "The agent should always escalate to a human for every refund to avoid errors",
      "B": "The agent should handle standard-policy refunds autonomously and escalate exception cases to a human with a structured summary of the request and why it falls outside policy",
      "C": "The agent should approve all refunds autonomously to maximize customer satisfaction",
      "D": "The agent should deny exception requests and only process standard ones"
    },
    "answer": "B",
    "explanation": "Good escalation design separates routine cases (handle autonomously) from edge cases (escalate with context). The escalation should include a structured handoff—summarizing the request, the policy conflict, and relevant customer data—so the human reviewer has full context. Always escalating wastes human time; never escalating risks policy violations."
  },
  {
    "domain": 3,
    "scenario": "A team is using the Message Batches API to process 10,000 product descriptions for classification. Each request is independent. They want to minimize cost while maintaining classification quality.",
    "question": "What is the primary advantage of using the Message Batches API over sending individual API requests for this workload?",
    "options": {
      "A": "Batches allow using a larger context window per request",
      "B": "Batches provide 50% cost reduction for asynchronous processing of independent requests",
      "C": "Batches guarantee faster response times than individual requests",
      "D": "Batches automatically retry failed requests with a different model"
    },
    "answer": "B",
    "explanation": "The Message Batches API offers a 50% cost discount for workloads that can tolerate asynchronous processing (results within 24 hours). Since the 10,000 classifications are independent and don't need real-time results, batching is ideal. Batches don't increase context windows, don't guarantee faster responses (they trade latency for cost), and don't auto-retry with different models."
  },
  {
    "domain": 1,
    "scenario": "A multi-agent customer support system uses a coordinator that delegates to specialist subagents. The architect must assign models to each role: a triage classifier, a policy-lookup agent, and a complex dispute resolution agent that reasons about edge cases.",
    "question": "Which model assignment best balances cost and capability across these roles?",
    "options": {
      "A": "Use Opus for all three roles to maximize quality uniformly",
      "B": "Haiku for the triage classifier (fast, cheap classification), Sonnet for policy lookup (balanced reasoning), and Opus for dispute resolution (deep multi-step reasoning on edge cases)",
      "C": "Sonnet for all roles as a safe middle ground",
      "D": "Use Haiku for all roles and add more retry loops to compensate for quality"
    },
    "answer": "B",
    "explanation": "Model selection should match capability to task complexity. Haiku excels at fast, low-cost classification (triage). Sonnet provides balanced reasoning for mid-complexity tasks (policy lookup). Opus is reserved for tasks requiring deep multi-step reasoning (dispute resolution with edge cases). Using the most capable model everywhere wastes budget; using the cheapest everywhere sacrifices quality on hard tasks."
  },
  {
    "domain": 2,
    "scenario": "A developer wants to create a reusable skill in Claude Code that performs security audits. The skill should run in an isolated context, only have access to Read and Grep tools, and be invocable by teammates via a slash command.",
    "question": "Where should the skill be defined and what configuration is needed?",
    "options": {
      "A": "Create a file in .claude/commands/ with 'context: fork' and 'allowed-tools: Read, Grep' in its YAML frontmatter",
      "B": "Add the skill to CLAUDE.md with inline tool restrictions",
      "C": "Create a file in ~/.claude/commands/ with no special frontmatter",
      "D": "Define the skill in settings.json under a 'skills' key"
    },
    "answer": "A",
    "explanation": "Custom skills/commands are defined as files in .claude/commands/ (project-level, version-controlled). YAML frontmatter supports 'context: fork' for isolated execution and 'allowed-tools' to restrict which tools the skill can use. User-level ~/.claude/commands/ works but is not shared with teammates. CLAUDE.md and settings.json do not support skill definitions."
  },
  {
    "domain": 2,
    "scenario": "A team lead wants to configure Claude Code so that junior developers cannot run Bash commands or edit files outside the src/ directory, while senior developers have full access. Both groups work on the same repo.",
    "question": "How should this permission differentiation be implemented?",
    "options": {
      "A": "Create two different CLAUDE.md files and tell each group which one to use",
      "B": "Use .claude/settings.local.json on each developer's machine to configure their personal permission levels, since this file is not committed to version control",
      "C": "Define permission levels in the project-level settings.json with role-based access control",
      "D": "Create PreToolUse hooks that check the developer's username before allowing tool execution"
    },
    "answer": "B",
    "explanation": "settings.local.json is the correct mechanism for per-developer configuration that should not be shared via version control. Each developer's machine has its own settings.local.json with appropriate permission levels. Project-level settings.json applies to everyone equally. CLAUDE.md instructions are advisory, not enforced. While hooks could work, settings.local.json is the idiomatic approach for personal permission configuration."
  },
  {
    "domain": 3,
    "scenario": "You need Claude to generate a product description that ends with a specific closing tag '</product>' and nothing after it. Without constraints, Claude sometimes appends follow-up commentary after the closing tag.",
    "question": "Which technique most reliably ensures the output stops immediately after the closing tag?",
    "options": {
      "A": "Add 'do not write anything after </product>' to the system prompt",
      "B": "Set a stop sequence of '</product>' so the API terminates generation immediately when that string is produced",
      "C": "Set max_tokens to a value that cuts off after the expected tag position",
      "D": "Use a low temperature to reduce verbosity"
    },
    "answer": "B",
    "explanation": "Stop sequences cause the API to halt generation as soon as the specified string is produced. This is a deterministic mechanism — more reliable than prompt instructions (probabilistic) or max_tokens (position-dependent). Stop sequences are ideal for controlling where output ends, while prefilling controls where it begins."
  },
  {
    "domain": 3,
    "scenario": "You are designing a complex prompt that includes system instructions, a document to analyze, few-shot examples, and the user's question. The prompt is getting difficult to maintain and Claude occasionally confuses the document content with the instructions.",
    "question": "What is the best structural technique to prevent this confusion?",
    "options": {
      "A": "Use numbered sections with headers like '1. Instructions' and '2. Document'",
      "B": "Use XML tags to clearly delimit each section (e.g., <instructions>, <document>, <examples>, <query>) so Claude can unambiguously identify the role of each content block",
      "C": "Put each section in a separate API call and chain the results",
      "D": "Use triple backticks to wrap each section"
    },
    "answer": "B",
    "explanation": "XML tags provide unambiguous structural delimiters that Claude is trained to respect. Tags like <instructions>, <document>, <examples>, and <query> clearly separate the role of each content block, preventing the model from confusing document content with instructions. This is the standard CCA-F technique for prompt organization. Numbered headers and backticks are less precise. Chaining adds unnecessary latency and complexity."
  },
  {
    "domain": 4,
    "scenario": "An MCP server exposes reusable prompt templates for common workflows — like 'summarize-for-executive' and 'extract-action-items' — that combine specific instructions with placeholders for user-provided content.",
    "question": "Which MCP primitive is the correct way to expose these templates?",
    "options": {
      "A": "MCP tools with the template text hardcoded in the tool description",
      "B": "MCP resources that return the template text as static content",
      "C": "MCP prompts — the dedicated primitive for reusable, parameterized prompt templates that users or clients can discover and invoke",
      "D": "Custom HTTP endpoints outside the MCP protocol"
    },
    "answer": "C",
    "explanation": "MCP defines three primitives: tools (actions with side effects), resources (read-only data), and prompts (reusable prompt templates). Prompts are the dedicated primitive for parameterized templates that combine instructions with user-provided arguments. Exposing templates as tools misuses the tool primitive; resources are for data, not interactive templates. Custom endpoints bypass MCP's discoverability."
  },
  {
    "domain": 4,
    "scenario": "A team wants to share MCP server configurations across their Claude Code project. They need to specify which servers to connect to, their transport settings, and environment variable references for credentials — all in version control.",
    "question": "What is the standard mechanism for project-level MCP server configuration in Claude Code?",
    "options": {
      "A": "Define MCP servers in the project-level CLAUDE.md file",
      "B": "Use a .mcp.json file at the project root that declares server names, transport types (stdio or HTTP), command/URL settings, and environment variable references for credentials",
      "C": "Configure MCP servers in settings.json under an 'mcp' key",
      "D": "Create individual config files per MCP server in a .claude/mcp/ directory"
    },
    "answer": "B",
    "explanation": ".mcp.json is the standard project-level configuration file for MCP servers in Claude Code. It declares server entries with their transport type, command (for stdio) or URL (for HTTP), arguments, and environment variable references for sensitive values. This file is committed to version control so all team members share the same server configuration."
  },
  {
    "domain": 5,
    "scenario": "A production Claude agent makes API calls that occasionally fail due to rate limits or transient network errors. The current implementation retries immediately on any error, which sometimes triggers more rate limit errors.",
    "question": "What is the recommended retry strategy for transient API failures?",
    "options": {
      "A": "Retry immediately up to 10 times — persistence overcomes transient failures",
      "B": "Implement exponential backoff with jitter, distinguish between retryable errors (rate limits, 5xx) and non-retryable errors (4xx validation), and set a maximum retry count",
      "C": "Never retry — fail fast and surface the error to the user immediately",
      "D": "Add a fixed 60-second delay between all retries regardless of error type"
    },
    "answer": "B",
    "explanation": "Exponential backoff with jitter prevents retry storms that worsen rate limiting. Distinguishing retryable (transient) from non-retryable (permanent) errors avoids wasting time on errors that won't resolve with retries. A maximum retry count prevents infinite loops. Immediate retries amplify rate limit pressure, fixed delays waste time on fast-recovering errors, and never retrying sacrifices availability unnecessarily."
  },
  {
    "domain": 5,
    "scenario": "A customer-facing Claude agent occasionally generates responses that contain profanity or off-topic content. The team wants a systematic architectural approach to prevent harmful outputs without relying solely on prompt instructions.",
    "question": "Which architectural pattern best addresses this concern?",
    "options": {
      "A": "Add 'never use profanity' to the system prompt and trust the model to comply",
      "B": "Implement input and output guardrails — validate user inputs before they reach the model and screen model outputs before they reach the user, using a combination of classifiers and rule-based filters",
      "C": "Lower the temperature to 0 to minimize unpredictable outputs",
      "D": "Use a smaller model that is less likely to generate creative content"
    },
    "answer": "B",
    "explanation": "Input/output guardrails provide a systematic, defense-in-depth approach to content safety. Input guardrails screen and sanitize user messages before processing. Output guardrails check model responses against safety classifiers and rule-based filters before delivery. This is more reliable than prompt-only defenses (probabilistic), temperature tuning (doesn't prevent harmful content), or model downgrades (reduce capability without guaranteeing safety)."
  },
  {
    "domain": 1,
    "scenario": "A multi-turn customer support agent needs to track the customer's current issue, previous actions taken, and pending next steps across many conversation turns. After 20+ turns, the agent loses track of what has already been tried and suggests repeating actions.",
    "question": "What pattern should be used to maintain reliable session state across a long multi-turn agentic interaction?",
    "options": {
      "A": "Rely on the conversation history — the model can read back through all previous turns",
      "B": "Maintain an explicit session state object (tracking current issue, actions taken, pending steps) that is updated after each turn and injected into the next prompt as structured context",
      "C": "Summarize the conversation every 5 turns and replace the history with the summary",
      "D": "Use a separate database to log every message and query it before each response"
    },
    "answer": "B",
    "explanation": "Explicit session state management — maintaining a structured object with current issue, completed actions, and pending steps — provides reliable tracking across long conversations. The state object is updated after each turn and injected as structured context, preventing the agent from losing track of progress. Relying on raw conversation history degrades as context grows. Periodic summarization risks losing action-level detail. Full database querying adds latency without structured reasoning support."
  },
  {
    "domain": 1,
    "scenario": "The CCA-F exam draws from a pool of six predefined scenarios, but each exam instance only presents four of them. A candidate is preparing and wants to allocate study time efficiently across the scenarios.",
    "question": "Which of the following is a correct list of the six CCA-F exam scenarios?",
    "options": {
      "A": "Customer Support Agent, Claude Code Team Setup, Multi-Agent Research System, Developer Productivity Tools, Claude Code in CI/CD, Structured Data Extraction",
      "B": "Customer Support Agent, RAG Pipeline, Multi-Agent Research System, API Gateway, Claude Code in CI/CD, Structured Data Extraction",
      "C": "Chatbot Design, Claude Code Team Setup, Multi-Agent Research System, Developer Productivity Tools, Fine-Tuning Pipeline, Structured Data Extraction",
      "D": "Customer Support Agent, Claude Code Team Setup, Embedding Pipeline, Developer Productivity Tools, Claude Code in CI/CD, Model Training"
    },
    "answer": "A",
    "explanation": "The CCA-F exam uses six predefined scenarios, of which four are randomly selected per exam: (1) Customer Support Agent, (2) Claude Code Team Setup, (3) Multi-Agent Research System, (4) Developer Productivity Tools, (5) Claude Code in CI/CD, and (6) Structured Data Extraction. Candidates should prepare for all six since they cannot predict which four will appear."
  },
  {
    "domain": 4,
    "scenario": "A team is migrating an existing MCP server from SSE (Server-Sent Events) transport to a newer protocol. The server is deployed behind a cloud load balancer and serves multiple concurrent clients.",
    "question": "Why is SSE being deprecated in favor of Streamable HTTP for new MCP server deployments?",
    "options": {
      "A": "SSE is slower than Streamable HTTP due to encoding overhead",
      "B": "SSE requires two separate endpoints (one for sending, one for receiving), while Streamable HTTP uses a single /mcp endpoint, simplifies load balancing with stateless design, and includes built-in session management via Mcp-Session-Id headers",
      "C": "SSE does not support JSON-RPC, which is required by MCP",
      "D": "SSE cannot handle more than 10 concurrent connections per server"
    },
    "answer": "B",
    "explanation": "SSE transport requires separate endpoints for client-to-server and server-to-client communication, complicating load balancer configuration. Streamable HTTP consolidates to a single /mcp endpoint, supports stateless request-response patterns (load-balancer friendly), and includes Mcp-Session-Id headers for session management. As of MCP 2025-05, Streamable HTTP is the recommended remote transport and SSE is deprecated for new servers."
  },
  {
    "domain": 4,
    "scenario": "An MCP server deployed with Streamable HTTP transport serves multiple concurrent clients behind a load balancer. The server needs to maintain per-client state (e.g., active subscriptions and cursor positions) across requests that may land on different backend instances.",
    "question": "How does the MCP protocol support session continuity in this environment?",
    "options": {
      "A": "MCP does not support sessions — each request is fully independent",
      "B": "The server assigns an Mcp-Session-Id header on the first response, and the client includes it in subsequent requests, allowing any backend instance to look up session state from shared storage",
      "C": "Sticky sessions at the load balancer level are the only supported mechanism",
      "D": "The client embeds full session state in every request body"
    },
    "answer": "B",
    "explanation": "Streamable HTTP transport includes built-in session management via the Mcp-Session-Id header. The server generates a session ID on initialization and returns it in the response header. The client includes this ID in all subsequent requests. This allows stateless load balancing — any backend instance can serve any request by looking up the session ID in shared storage (e.g., Redis). Sticky sessions are not required."
  },
  {
    "domain": 1,
    "scenario": "A developer is building a Claude-based agent using the Claude Agent SDK. The agent needs to hand off a conversation from a general-purpose triage agent to a specialized billing agent mid-conversation, preserving the conversation context.",
    "question": "How should agent handoffs be implemented in a multi-agent system?",
    "options": {
      "A": "Start a completely new conversation with the billing agent and re-send all messages",
      "B": "The triage agent returns a structured handoff signal with the target agent identifier and a context summary, and the orchestrator routes the conversation to the billing agent with the relevant context attached",
      "C": "Both agents run simultaneously and the user picks which response to accept",
      "D": "The triage agent modifies its own system prompt to behave like the billing agent"
    },
    "answer": "B",
    "explanation": "Agent handoffs in multi-agent systems use structured signals: the current agent returns a handoff indicator (target agent ID + context summary), and the orchestrator routes to the target agent with relevant context. This preserves conversation continuity without re-sending all messages. Having agents modify their own prompts defeats the purpose of specialization, and running both simultaneously wastes resources."
  },
  {
    "domain": 4,
    "scenario": "During MCP server initialization, the client and server exchange capabilities to determine which protocol features each side supports. A developer skips this negotiation and immediately starts sending tool/list requests.",
    "question": "What will likely happen?",
    "options": {
      "A": "The requests will succeed because capability negotiation is optional",
      "B": "The server may reject requests or behave unpredictably because the MCP protocol requires an initialize handshake where client and server exchange supported capabilities before any other requests",
      "C": "The client will automatically fall back to a default capability set",
      "D": "Only tool requests will fail; resource requests will work without initialization"
    },
    "answer": "B",
    "explanation": "MCP requires an initialization handshake (initialize request/response) before any other protocol messages. During this exchange, the client declares its capabilities (e.g., roots support, sampling support) and the server declares its capabilities (e.g., tools, resources, prompts). Skipping initialization violates the protocol and will cause the server to reject subsequent requests or behave unpredictably."
  },
  {
    "domain": 2,
    "scenario": "A developer is troubleshooting why their Claude Code custom slash command's intermediate tool calls and verbose output are cluttering the main conversation, making it hard to see the final result.",
    "question": "What configuration change would isolate the command's execution from the main conversation?",
    "options": {
      "A": "Add 'verbose: false' to the command's YAML frontmatter",
      "B": "Add 'context: fork' to the command's YAML frontmatter so it runs in a forked subagent context, returning only the final result to the main conversation",
      "C": "Move the command from .claude/commands/ to .claude/background/",
      "D": "Wrap the command output in <hidden> XML tags"
    },
    "answer": "B",
    "explanation": "The 'context: fork' frontmatter directive causes the command to execute in a forked context — a separate subagent that inherits the current context but operates independently. Only the final result is returned to the main conversation, keeping intermediate tool calls and verbose output isolated. This is the standard mechanism for clean command output."
  },
  {
    "domain": 1,
    "scenario": "An architect is designing a pipeline where Claude generates marketing copy, then a separate evaluation step checks the copy against brand guidelines. If it fails, Claude revises. This cycle repeats until the copy passes or a maximum of 3 iterations is reached.",
    "question": "What is the name of this agentic pattern?",
    "options": {
      "A": "Prompt chaining",
      "B": "Evaluator-optimizer loop",
      "C": "Routing pattern",
      "D": "Fan-out/fan-in parallelization"
    },
    "answer": "B",
    "explanation": "The evaluator-optimizer pattern uses two roles: a generator that produces output and an evaluator that scores it against criteria. The loop iterates until quality criteria are met or a max-iteration cap is reached. Prompt chaining is a linear sequence without feedback loops. Routing dispatches to different handlers. Fan-out runs tasks in parallel. The iterative generate-evaluate-revise cycle with a termination condition defines the evaluator-optimizer."
  },
  {
    "domain": 5,
    "scenario": "A team deploys a Claude agent that answers customer questions using a knowledge base. They measure accuracy using only automated tests with known question-answer pairs. After deployment, customers report frequent incorrect answers on questions not in the test set.",
    "question": "What evaluation gap caused this production quality issue?",
    "options": {
      "A": "The test set was too large, causing overfitting of the evaluation",
      "B": "The evaluation lacked out-of-distribution testing and live monitoring — fixed test suites do not capture the full variability of real-world inputs, and production performance must be continuously monitored",
      "C": "Automated tests are inherently unreliable and should never be used",
      "D": "The knowledge base was too small to cover all possible questions"
    },
    "answer": "B",
    "explanation": "Fixed test suites create false confidence by only covering known scenarios. Production-ready evaluation requires: out-of-distribution test cases that simulate unexpected real-world inputs, ongoing live monitoring with alerting for accuracy degradation, and regression baselines. The CCA-F emphasizes that static evaluations alone are insufficient — continuous production monitoring is essential for reliability."
  },
  {
    "domain": 3,
    "scenario": "A developer wants to extract data from 500 customer feedback forms using Claude. The forms are identical in structure, results are needed by end of day, and cost is a primary concern. Real-time processing is not required.",
    "question": "Which API approach should the developer use?",
    "options": {
      "A": "Send all 500 requests synchronously in a loop for simplicity",
      "B": "Use streaming mode for each request to get results faster",
      "C": "Use the Message Batches API to submit all 500 requests as a batch for up to 50% cost savings",
      "D": "Use extended thinking on each request to improve extraction quality"
    },
    "answer": "C",
    "explanation": "The Message Batches API is designed for exactly this use case: large volumes of independent, latency-tolerant requests. It provides up to 50% cost savings with results delivered within 24 hours. Since the forms are identical in structure and results aren't needed in real-time, batch processing is optimal. Synchronous loops waste time, streaming doesn't reduce cost, and extended thinking adds cost without addressing the batch processing need."
  },
  {
    "domain": 5,
    "scenario": "A Claude-based coding agent uses extended thinking for complex debugging tasks. The team notices that token costs have increased significantly compared to non-thinking mode, even though cached prompts are being reused.",
    "question": "Why do costs increase when extended thinking is used with prompt caching?",
    "options": {
      "A": "Extended thinking disables prompt caching, so all tokens are billed at full price",
      "B": "Thinking tokens are billed as output tokens (which are more expensive than input tokens), and when thinking blocks from previous turns are cached and read back, they count as additional input tokens — creating a compounding cost effect in multi-turn conversations",
      "C": "Extended thinking doubles the context window size, doubling all token costs",
      "D": "Cached thinking tokens are billed at 3x the normal rate as a premium feature"
    },
    "answer": "B",
    "explanation": "Extended thinking tokens are billed as output tokens (more expensive than input). In multi-turn conversations with caching, thinking blocks from previous assistant turns become part of the cached input, adding to input token costs when read from cache. This creates a cost tradeoff: better reasoning quality from thinking vs. higher cumulative costs from both the thinking output tokens and their subsequent cached input reads."
  },
  {
    "domain": 1,
    "scenario": "An architect is building a multi-agent system using the Managed Agents API. The coordinator agent delegates to a code reviewer and a test writer. During testing, they notice the coordinator tries to have the code reviewer delegate a sub-task to a linting agent, but the linting agent is never invoked.",
    "question": "Why does the nested delegation fail?",
    "options": {
      "A": "The linting agent was not added to the coordinator's roster",
      "B": "The Managed Agents API enforces a one-level-deep delegation limit — the coordinator can delegate to agents in its roster, but those agents cannot spawn further subagents. Depth > 1 is ignored.",
      "C": "The code reviewer needs its own agent_toolset_20260401 tool to delegate",
      "D": "Nested delegation requires a separate session for each level"
    },
    "answer": "B",
    "explanation": "The Managed Agents API enforces a strict one-level-deep delegation limit. The coordinator can delegate to agents in its multiagent.agents roster, but those agents cannot spawn their own subagents — depth > 1 is ignored. If the linting capability is needed, it must be added directly to the coordinator's roster or incorporated into the code reviewer's own tools. This constraint simplifies reasoning about agent topologies and prevents unbounded delegation chains."
  },
  {
    "domain": 1,
    "scenario": "A multiagent session has a coordinator delegating to 22 specialized agents. After spawning threads for each, the coordinator attempts to delegate to 3 more agents for a follow-up task but the new thread creation fails.",
    "question": "What is the likely cause and the correct mitigation?",
    "options": {
      "A": "The coordinator's roster is limited to 20 agents, so 22 is already over the limit",
      "B": "Multiagent sessions support a maximum of 25 concurrent threads; the coordinator should archive completed threads to free slots before spawning new ones",
      "C": "Each agent can only be invoked once per session",
      "D": "The session has exceeded its token budget"
    },
    "answer": "B",
    "explanation": "Multiagent sessions support a maximum of 25 concurrent threads. While the roster can contain up to 20 unique agents, the coordinator can call multiple copies of each, creating multiple threads. When the 25-thread limit is reached, completed threads must be archived to free slots. Archiving only succeeds on idle threads — running threads must be interrupted first. The roster limit (20 agents) and thread limit (25 concurrent) are separate constraints."
  },
  {
    "domain": 1,
    "scenario": "In a multiagent session, the coordinator delegates a code review to Agent A. Later, the coordinator needs Agent A to perform a follow-up review on updated code. A developer assumes a new thread must be created for the follow-up.",
    "question": "Is a new thread necessary for the follow-up?",
    "options": {
      "A": "Yes — each delegation always creates a new thread with fresh context",
      "B": "No — threads are persistent. The coordinator can send a follow-up message to Agent A's existing thread, and Agent A retains its full conversation history from the first review",
      "C": "No — but the coordinator must manually re-inject the previous context into the follow-up message",
      "D": "Yes — threads are ephemeral and destroyed after each response"
    },
    "answer": "B",
    "explanation": "Threads in the Managed Agents API are persistent. When the coordinator sends a follow-up to an agent it previously delegated to, that agent retains everything from its previous turns — conversation history, tool results, and reasoning context. This enables iterative workflows (review → feedback → re-review) without re-sending context or creating new threads. The coordinator does not need to re-inject prior context; the thread preserves it automatically."
  },
  {
    "domain": 1,
    "scenario": "A multiagent system uses a coordinator with three specialist agents. All agents share the same sandbox and filesystem but run in their own session threads. A developer is confused about what is shared vs. isolated between agents.",
    "question": "What is shared and what is isolated between agents in a multiagent session?",
    "options": {
      "A": "Everything is shared — agents see each other's conversation history, tools, and file changes",
      "B": "Nothing is shared — each agent has its own sandbox, filesystem, and context",
      "C": "Agents share the sandbox, filesystem, and vault credentials, but each agent's conversation history, tools, MCP servers, and system prompt are isolated to its own thread",
      "D": "Agents share conversation history but have isolated filesystems"
    },
    "answer": "C",
    "explanation": "In the Managed Agents API, all agents in a session share the same sandbox, filesystem, and vault credentials. However, each agent runs in its own session thread with isolated context — its own conversation history, model, system prompt, tools, and MCP servers as defined in its agent configuration. This means Agent A can write a file that Agent B reads, but Agent A cannot see Agent B's conversation or use Agent B's tools. Vault credentials are session-scoped (available to all threads), while MCP servers are agent-scoped."
  },
  {
    "domain": 1,
    "scenario": "An architect is building a long-running agent using the Claude Agent SDK. The agent processes customer onboarding workflows that may take hours. The developer stores all agent state in memory and assumes the SDK session will persist for the full duration.",
    "question": "What is the architectural flaw in this approach?",
    "options": {
      "A": "The SDK session will time out after 30 minutes regardless of activity",
      "B": "SDK sessions should be treated as ephemeral — all messages, tool calls, and results must be persisted to durable storage (Postgres, Redis, or object storage) so the agent can resume after crashes, deploys, or process restarts",
      "C": "In-memory state is fine as long as the process is monitored",
      "D": "The developer should use a larger instance with more memory"
    },
    "answer": "B",
    "explanation": "A core production pattern for the Claude Agent SDK is treating sessions as ephemeral. Storing state only in memory means a crash, deploy, or restart loses all progress. The conversation log should be persisted to durable storage (Postgres, Redis, object storage) after each tool call, enabling resume-from-checkpoint. The conversation log serves as the contract between agent turns and across handoffs — it is the source of truth, not the in-memory session."
  },
  {
    "domain": 1,
    "scenario": "A Claude-based agent enters a loop where it repeatedly calls the same tool with identical arguments, receiving the same error each time. After 15 iterations, the agent has consumed significant tokens without making progress.",
    "question": "What production safeguard should prevent this behavior?",
    "options": {
      "A": "Lower the temperature to 0 to prevent the agent from retrying",
      "B": "Implement a circuit breaker that sets a numeric iteration limit (e.g., 25) and detects repetition by hashing tool calls — tripping when identical calls repeat 3 times, then surfacing the failure rather than silently retrying",
      "C": "Add a system prompt instruction saying 'do not retry failed tool calls'",
      "D": "Set a global timeout that kills the process after 5 minutes"
    },
    "answer": "B",
    "explanation": "Circuit breakers are an essential production safeguard for agentic systems. They combine two mechanisms: a hard iteration cap (e.g., 25 iterations) and repetition detection (hashing tool call signatures and tripping when identical calls repeat N times). When tripped, the failure should be surfaced explicitly — not silently retried. System prompt instructions are probabilistic and unreliable for preventing loops. Global timeouts are too blunt and waste tokens up to the timeout."
  },
  {
    "domain": 1,
    "scenario": "A multi-tenant SaaS platform runs Claude agents for different customers. Each customer's agent should only have access to tools relevant to their subscription tier. A developer gives every agent the full set of 30 available tools and relies on system prompt instructions to restrict usage per tenant.",
    "question": "What is the security and reliability concern with this approach?",
    "options": {
      "A": "30 tools is fine — Claude handles large tool sets well",
      "B": "Giving agents the union of all capabilities violates principle of least privilege. Tools should be scoped per session, task type, and tenant — never the full union. Prompt-based restrictions are probabilistic and can be bypassed.",
      "C": "The concern is only cost — unnecessary tools add token overhead but pose no security risk",
      "D": "System prompt restrictions are sufficient if they are detailed enough"
    },
    "answer": "B",
    "explanation": "Tool permissioning should follow the principle of least privilege: scope available tools to the specific session, task type, and tenant. Giving every agent the union of all capabilities creates both security risks (an agent might access tools outside its tenant's authorization) and reliability risks (too many tools degrades tool selection accuracy). Prompt-based restrictions are probabilistic — they can be bypassed through prompt injection or model error. Programmatic tool scoping is the correct enforcement mechanism."
  },
  {
    "domain": 1,
    "scenario": "An architect is choosing between five multi-agent orchestration patterns for a customer support system: fan-out, pipeline, debate, supervisor, and swarm. The system needs a lead agent that breaks work into pieces, delegates to specialists, and synthesizes results with clear accountability.",
    "question": "Which pattern best fits this requirement?",
    "options": {
      "A": "Fan-out — scatter tasks to all agents and gather results",
      "B": "Pipeline — chain agents sequentially, each transforming the output",
      "C": "Supervisor — a lead agent delegates to specialists, monitors progress, and synthesizes results with hierarchical accountability",
      "D": "Swarm — dynamic peer agents self-organize without a central coordinator"
    },
    "answer": "C",
    "explanation": "The supervisor (coordinator-worker) pattern fits scenarios requiring hierarchical delegation with clear accountability. The lead agent owns the task lifecycle: decomposing work, delegating to specialists, monitoring progress, and synthesizing results. Fan-out is a sub-pattern of supervisor (parallel dispatch without ongoing oversight). Pipeline is for sequential transformations. Debate is for multi-perspective critique. Swarm lacks the centralized accountability the requirement demands. Supervisor is the 2026 production default for most enterprise multi-agent systems."
  },
  {
    "domain": 1,
    "scenario": "A coordinator agent's multiagent roster references Agent B at version 3. A week later, the team updates Agent B to version 4 with improved instructions and a new tool. The coordinator continues to delegate to Agent B but the new tool is never available.",
    "question": "Why is the coordinator still using the old version of Agent B?",
    "options": {
      "A": "The coordinator automatically uses the latest version after a 24-hour cache refresh",
      "B": "The coordinator's roster is snapshotted at creation time — referenced agents are pinned to the versions resolved when the coordinator was created. To use version 4, the coordinator must be updated so its roster references the new version.",
      "C": "Agent versions are immutable and cannot be updated after creation",
      "D": "The coordinator uses whichever version was first invoked in the current session"
    },
    "answer": "B",
    "explanation": "The Managed Agents API snapshots the coordinator's configuration, including its multiagent.agents roster, at creation time. Referenced agents are pinned to the versions resolved at that point and do not automatically pick up later updates. To delegate to a newer version of an agent, the coordinator must be explicitly updated so its roster references the new version. This design ensures deterministic behavior — the coordinator's dependencies don't change unexpectedly."
  },
  {
    "domain": 4,
    "scenario": "In a multiagent session, a subagent needs to call an MCP server that requires authentication via a vault credential. The architect configured the MCP server in the subagent's definition but forgot to include the vault credential at session creation.",
    "question": "Why do the subagent's MCP calls fail to authenticate?",
    "options": {
      "A": "MCP servers cannot be used by subagents, only by the coordinator",
      "B": "MCP servers are agent-scoped (declared per agent), but vault credentials are session-scoped (passed at session creation and shared across all threads). The missing vault_ids at session creation means no thread has access to the credential.",
      "C": "Each agent must declare its own vault credentials in its agent definition",
      "D": "The MCP server URL in the vault credential does not match the agent's server URL"
    },
    "answer": "B",
    "explanation": "In the Managed Agents API, MCP servers are agent-scoped — each agent declares which servers it uses. However, vault credentials are session-scoped — they are passed via vault_ids at session creation and apply to all threads. If a vault credential is not included at session creation, no agent in the session can authenticate with the corresponding MCP server. The vault credential's mcp_server_url must also match the agent's mcp_servers[].url exactly, including scheme and trailing slash."
  },
  {
    "domain": 1,
    "scenario": "A production agentic system needs three tiers of spending controls: protection against runaway loops on individual tasks, abuse prevention per user, and budget enforcement per tenant in a multi-tenant environment.",
    "question": "Which cost control architecture satisfies all three requirements?",
    "options": {
      "A": "Set a single global API spending limit and monitor usage dashboards",
      "B": "Implement three-tier hard cost caps checked before every API call: per-task ceilings (runaway loop protection), per-user daily/monthly limits (abuse prevention), and per-tenant monthly budgets (multi-tenant safety)",
      "C": "Use billing alerts that notify the team when spending exceeds thresholds",
      "D": "Limit max_tokens on each API call to cap individual request costs"
    },
    "answer": "B",
    "explanation": "Production agentic systems require three-tier spending controls: per-task ceilings prevent runaway loops from consuming unbounded tokens, per-user limits prevent individual abuse, and per-tenant budgets enforce commercial boundaries in multi-tenant systems. These must be hard caps checked before every API call — not alerts (which notify after overspend) or single global limits (which don't isolate failures). max_tokens caps individual response size but doesn't prevent loop-driven cost accumulation."
  },
  {
    "domain": 2,
    "scenario": "A security team wants a PreToolUse hook that intercepts Bash tool calls. For most flagged commands, the hook should block execution outright. However, for borderline commands (e.g., 'git push --force' to a non-protected branch), the hook should defer to the user for a manual approval decision rather than auto-blocking.",
    "question": "How should the hook's JSON output differ between these two cases?",
    "options": {
      "A": "Use exit code 2 for both cases — Claude Code always prompts the user on exit 2",
      "B": "Return hookSpecificOutput with permissionDecision 'deny' for definite blocks, and permissionDecision 'ask' for borderline commands that should escalate to the user's permission dialog",
      "C": "Return hookSpecificOutput with permissionDecision 'allow' for borderline commands so the user can decide later",
      "D": "Use exit code 0 for borderline and exit code 1 for definite blocks"
    },
    "answer": "B",
    "explanation": "PreToolUse hooks support four permission decisions: 'allow' (approve), 'deny' (block), 'ask' (escalate to user permission dialog), and 'defer' (let normal permission flow apply). For definite blocks, 'deny' prevents execution unconditionally. For borderline cases, 'ask' escalates to the user's interactive permission prompt, giving humans the final say. Exit code 2 blocks without user choice; 'allow' would approve without user input."
  },
  {
    "domain": 2,
    "scenario": "A team's PostToolUse hook inspects the output of Read tool calls on files matching '*.env'. The hook needs to redact any lines containing API keys before Claude processes the content, while also injecting a note telling Claude that the file was redacted.",
    "question": "Which combination of JSON output fields achieves both goals?",
    "options": {
      "A": "Use 'decision: block' to prevent Claude from seeing the output entirely",
      "B": "Use hookSpecificOutput.updatedToolOutput to replace the raw file content with the redacted version, and hookSpecificOutput.additionalContext to inject a note like 'API keys were redacted from this file'",
      "C": "Use systemMessage to display the redacted content to the user, and suppressOutput to hide it from Claude",
      "D": "Use a PreToolUse hook instead to prevent the Read from executing"
    },
    "answer": "B",
    "explanation": "PostToolUse hooks can modify what Claude sees via updatedToolOutput (replaces the tool's raw result) and inject contextual guidance via additionalContext (appears as a system reminder). This combination lets the hook redact sensitive data while informing Claude that redaction occurred. Blocking entirely loses the non-sensitive content. PreToolUse would prevent reading the file at all, which is too restrictive. systemMessage displays to the user, not to Claude."
  },
  {
    "domain": 3,
    "scenario": "A developer enables extended thinking on a Claude API call that also uses forced tool choice (tool_choice: {type: 'tool', name: 'validate_input'}) to guarantee a specific tool is called. The API returns a 400 error.",
    "question": "What is the cause of this error?",
    "options": {
      "A": "Extended thinking cannot be used with any tool_choice setting",
      "B": "Extended thinking only supports tool_choice 'auto' and 'none' — forced tool selection (type: 'tool' or type: 'any') is not compatible with extended thinking",
      "C": "The budget_tokens parameter was set too high for tool use",
      "D": "Extended thinking requires streaming mode, which conflicts with tool use"
    },
    "answer": "B",
    "explanation": "Extended thinking restricts tool_choice to 'auto' (default) and 'none'. Forced tool selection ('any' or specific tool name) is not supported because extended thinking needs the freedom to reason about whether and which tools to call. Attempting forced tool choice with thinking enabled returns a 400 error. The workaround is to use 'auto' and guide tool selection through the prompt instead."
  },
  {
    "domain": 3,
    "scenario": "An agentic loop uses extended thinking with tool use. After Claude calls a tool and receives the result, the developer constructs the next API request but strips the thinking blocks from the previous assistant turn to save input tokens.",
    "question": "What will happen when this request is sent?",
    "options": {
      "A": "The request succeeds but Claude loses its reasoning chain, producing lower-quality follow-up reasoning",
      "B": "The request fails with an invalid_request_error because thinking blocks must be preserved unchanged in multi-turn conversations with tool use — they carry encrypted signatures needed for continuity",
      "C": "The request succeeds and Claude automatically regenerates the missing thinking",
      "D": "The request succeeds but thinking is silently disabled for the remainder of the conversation"
    },
    "answer": "B",
    "explanation": "Thinking blocks contain a 'signature' field with encrypted full thinking content required for multi-turn continuity. When continuing a conversation (especially with tool results), thinking blocks from previous assistant turns MUST be passed back unchanged. Stripping or modifying them causes an invalid_request_error. This is a common production pitfall — developers must preserve thinking blocks even when using display: 'omitted' mode where the thinking text is empty but the signature is still required."
  },
  {
    "domain": 4,
    "scenario": "An MCP server needs to ask the host's LLM to classify a document before deciding which processing tool to invoke. The server does not have its own LLM access — it must leverage the model already connected to the MCP host.",
    "question": "Which MCP capability enables this pattern?",
    "options": {
      "A": "The server should expose a classification tool and let the host call it",
      "B": "MCP sampling — a server capability that allows the server to request LLM completions from the host, with user approval, enabling the server to leverage the host's model for intermediate reasoning",
      "C": "The server should make direct API calls to Claude using its own credentials",
      "D": "MCP resources — the server reads a pre-computed classification from a resource"
    },
    "answer": "B",
    "explanation": "MCP sampling is a protocol capability that lets servers request LLM completions from the host. This enables server-side workflows that need intermediate model reasoning (e.g., classify-then-route) without the server needing its own LLM access. The host controls which sampling requests to approve, maintaining human-in-the-loop oversight. This is distinct from tools (which the host calls on the server) and resources (which are static data)."
  },
  {
    "domain": 4,
    "scenario": "An MCP server deployed in a CI environment needs to restrict its file operations to only the repository checkout directory. The host (Claude Code) should communicate this boundary to the server at connection time.",
    "question": "Which MCP capability enforces this operational boundary?",
    "options": {
      "A": "File path validation in each tool's implementation",
      "B": "MCP roots — the client declares which directories or URIs the server is allowed to operate on, enforcing principle of least privilege at the protocol level",
      "C": "A PreToolUse hook that checks file paths before each tool call",
      "D": "An allowlist in the .mcp.json configuration file"
    },
    "answer": "B",
    "explanation": "MCP roots is a client capability declared during initialization that tells the server which directories or URIs it may operate within. This enforces operational boundaries at the protocol level — the server knows its allowed scope upfront rather than discovering it through errors. While tool-level validation and hooks provide defense-in-depth, roots is the protocol-native mechanism for communicating operational boundaries. It follows the principle of least privilege by restricting server access before any operations begin."
  },
  {
    "domain": 1,
    "scenario": "An architect is building a document analysis pipeline. The input documents are 500 pages (~125K tokens) and fit within Claude's 200K context window. A junior developer proposes chunking the documents into 50 segments, processing each with a separate API call, and merging the results.",
    "question": "What is the architectural problem with the chunking approach in this case?",
    "options": {
      "A": "Chunking is always better because it enables parallel processing",
      "B": "Over-chunking documents that fit in the context window loses cross-section relationships, introduces merge complexity, and adds unnecessary latency and cost — the document should be processed in a single call when it fits",
      "C": "The chunk size of 50 is too small; 10 chunks would be optimal",
      "D": "Chunking requires extended thinking, which adds cost"
    },
    "answer": "B",
    "explanation": "A common anti-pattern is reflexively chunking documents that actually fit within the context window. When a 125K-token document fits in a 200K-token window, single-call processing preserves cross-section relationships, avoids merge complexity, and reduces latency. Chunking is appropriate when documents exceed the context window, but unnecessary chunking fragments context, introduces merge logic, and multiplies API calls without benefit."
  },
  {
    "domain": 1,
    "scenario": "A multi-agent system fans out five research tasks to parallel subagents. Each subagent writes interim findings to a shared Salesforce record. Two subagents simultaneously update the same record field, and one update silently overwrites the other.",
    "question": "What design principle was violated?",
    "options": {
      "A": "The subagents should have used a smaller model to reduce race conditions",
      "B": "Parallelization can break stateful operations — parallel subagents must not perform concurrent writes to shared mutable state without coordination (e.g., append-only patterns, locks, or coordinator-mediated writes)",
      "C": "The subagents should have been run sequentially instead of in parallel",
      "D": "The Salesforce API should handle concurrency automatically"
    },
    "answer": "B",
    "explanation": "Parallelization is powerful for independent tasks but dangerous for shared mutable state. When parallel subagents write to the same resource, race conditions cause silent data loss. The fix is not to abandon parallelism but to coordinate writes: use append-only patterns (each subagent writes to its own field/record), have the coordinator aggregate results and perform a single write, or use optimistic locking. The CCA-F tests understanding of when parallelism is safe vs. when it requires coordination."
  },
  {
    "domain": 5,
    "scenario": "A production system uses prompt caching with a stable system prompt and few-shot examples. The team changes the extended thinking budget_tokens from 4000 to 8000 mid-session to handle a harder query. They notice their message-level cache breakpoints miss, but system prompt cache still hits.",
    "question": "Why does the system prompt cache survive the budget change while message caches are invalidated?",
    "options": {
      "A": "System prompts are never affected by any parameter changes",
      "B": "System prompt cache_control breakpoints are preserved independently of thinking parameter changes, but message-level cache breakpoints are invalidated because changing budget_tokens alters the request prefix that message caches depend on",
      "C": "Message caches have shorter TTLs than system prompt caches",
      "D": "The system prompt was smaller and therefore cached differently"
    },
    "answer": "B",
    "explanation": "Prompt caching uses prefix matching. System prompt caching operates independently from message-level caching — system prompt cache breakpoints survive thinking parameter changes. However, message-level cache breakpoints depend on the full request prefix including thinking configuration. When budget_tokens changes, the message prefix changes, invalidating message-level caches. This is a key nuance: teams can safely tune thinking budgets per-request without losing system prompt cache benefits, but multi-turn message caches will miss."
  },
  {
    "domain": 2,
    "scenario": "An architect wants to use a Stop hook that prevents Claude Code from ending its turn until all unit tests pass. The hook runs 'npm test', and if tests fail, it exits with code 2 and sends the failure output in stderr.",
    "question": "What is the effect of a Stop hook exiting with code 2?",
    "options": {
      "A": "Claude stops normally and the test failures are logged but ignored",
      "B": "Claude is prevented from stopping — the hook blocks the stop action, and the stderr content is fed back to Claude as an error message, prompting it to fix the failing tests before trying to stop again",
      "C": "Claude retries the stop action after a 30-second delay",
      "D": "The entire Claude Code session terminates with an error"
    },
    "answer": "B",
    "explanation": "Exit code 2 from a Stop hook blocks Claude from completing its turn. The stderr output is fed back to Claude as an error message, effectively telling it 'you cannot stop yet — here is why.' Claude then continues working, typically addressing the test failures, and attempts to stop again on a subsequent turn. This creates a deterministic quality gate: Claude cannot declare 'done' until the Stop hook's conditions (passing tests) are satisfied."
  },
  {
    "domain": 5,
    "scenario": "A production Claude agent answers questions from a knowledge base. The team tracks aggregate accuracy at 94%. A detailed audit reveals: billing questions score 99%, shipping questions score 95%, but return-policy questions score only 68%. The team had been reporting the 94% aggregate to stakeholders.",
    "question": "What are the two reliability anti-patterns present in this evaluation?",
    "options": {
      "A": "The aggregate metric is too high and the team should use a lower threshold",
      "B": "Aggregate accuracy masks per-category failures (return-policy is well below pass bar), and the team lacks per-category routing for low-confidence categories — they should track accuracy by category and route the failing category to human review or a specialized handler",
      "C": "The team should remove return-policy questions from the test set to improve the aggregate score",
      "D": "The knowledge base needs more return-policy documents, which will automatically fix accuracy"
    },
    "answer": "B",
    "explanation": "Two anti-patterns are present: (1) aggregate metrics masking per-category failures — 94% overall hides a 68% failure rate on returns, which is well below the 72% pass bar; and (2) no per-category escalation or routing — the failing category should be routed to human review or a specialized handler rather than being served by the same generic pipeline. The CCA-F emphasizes tracking accuracy by document type/category and using field-level or category-level confidence to route difficult cases."
  },
  {
    "domain": 3,
    "scenario": "A developer uses extended thinking with display: 'omitted' to reduce streaming latency. They notice the thinking field in the response is empty, but a 'signature' field contains an opaque string. In a follow-up turn, they need to pass this assistant response back.",
    "question": "What is the purpose of the signature field, and what happens if the developer omits the thinking block entirely from the follow-up request?",
    "options": {
      "A": "The signature is a debugging artifact and can be safely omitted — the model regenerates its reasoning automatically",
      "B": "The signature carries encrypted full thinking content needed for multi-turn continuity; omitting the thinking block (even with empty thinking text) from subsequent requests breaks the reasoning chain and causes an invalid_request_error",
      "C": "The signature is used only for billing verification and has no effect on reasoning",
      "D": "The signature enables prompt caching of thinking tokens and can be omitted if caching is not needed"
    },
    "answer": "B",
    "explanation": "When display is 'omitted', thinking blocks return with empty thinking text but a populated signature field containing encrypted full thinking. This signature is essential for multi-turn continuity — it lets the model reconstruct its prior reasoning chain. The thinking block (with its signature) MUST be passed back unchanged in follow-up requests. Omitting or modifying it causes an invalid_request_error. This is true even though the thinking text appears empty — the signature carries the actual reasoning state."
  },
  {
    "domain": 1,
    "scenario": "[Scenario 1: Customer Support Agent] A Claude-based customer support agent has access to MCP tools: get_customer, lookup_order, process_refund, and escalate_to_human. A customer asks for a refund on an order. The agent calls lookup_order and receives an empty result set with status 'success'. The agent interprets this as 'no order found' and tells the customer their order doesn't exist. The customer insists they placed the order yesterday.",
    "question": "What is the most likely design flaw causing this incorrect response?",
    "options": {
      "A": "The agent should have used a different model for order lookups",
      "B": "The lookup_order tool does not distinguish between 'no matching results' and 'access failure' — the tool should return structured responses that explicitly differentiate empty results from errors, including the query that was executed",
      "C": "The agent should always escalate when no order is found",
      "D": "The customer should provide their order number before the agent searches"
    },
    "answer": "B",
    "explanation": "In the Customer Support Agent scenario, tool responses must clearly distinguish between valid empty results ('no orders match this query') and access failures ('could not reach the order database'). An empty result with status 'success' is ambiguous — was the query correct? Did it search the right system? The tool should return structured metadata including resultCount, status, and the executed query so the agent can reason about whether the search was comprehensive or needs refinement."
  },
  {
    "domain": 5,
    "scenario": "[Scenario 1: Customer Support Agent] A customer support agent handles returns, billing disputes, and account issues. The target is 80%+ first-contact resolution. A customer expresses frustration using profanity but has a straightforward billing question that the agent can resolve. The agent's current logic escalates any conversation with negative sentiment to a human.",
    "question": "Why is this escalation logic flawed, and what should trigger escalation instead?",
    "options": {
      "A": "The agent should escalate even faster when profanity is detected",
      "B": "Sentiment-based escalation is an anti-pattern — it wastes human agent time on resolvable issues. Valid escalation triggers are: explicit customer requests for a human, policy gaps the agent cannot resolve, or genuine inability to make progress on the issue",
      "C": "The agent should lower its confidence threshold when negative sentiment is detected",
      "D": "The agent should respond to profanity with a warning before continuing"
    },
    "answer": "B",
    "explanation": "Sentiment-based escalation is a frequently tested anti-pattern in the Customer Support Agent scenario. An angry customer with a simple billing question doesn't need a human — the agent can resolve it. Escalating on sentiment wastes human resources and reduces the 80%+ first-contact resolution target. Correct triggers are: (1) explicit customer request for a human (honor immediately, no negotiation), (2) policy gaps, (3) genuine inability to progress. Self-reported confidence scores are also an anti-pattern trigger."
  },
  {
    "domain": 2,
    "scenario": "[Scenario 2: Claude Code Team Setup] A team is onboarding 8 developers to a Claude Code project. Senior devs have extensive personal ~/.claude/CLAUDE.md files with coding preferences. The project has a root CLAUDE.md and .claude/rules/ with path-specific rules. Junior developers report that Claude Code gives them different suggestions than senior devs on the same files.",
    "question": "What is causing the inconsistency, and how should the team fix it?",
    "options": {
      "A": "Junior devs should copy senior devs' personal CLAUDE.md files",
      "B": "Personal ~/.claude/CLAUDE.md files are not shared via version control. Team conventions must be codified in the project-level CLAUDE.md and .claude/rules/ directory, which are committed to the repo and apply to everyone equally",
      "C": "The inconsistency is caused by different Claude Code versions across machines",
      "D": "Each developer should use settings.json to override the project-level rules"
    },
    "answer": "B",
    "explanation": "In the Claude Code Team Setup scenario, a key gotcha is that user-level ~/.claude/CLAUDE.md is personal and not shared. Only project-level CLAUDE.md and .claude/rules/ files are committed to the repo. To ensure consistent behavior across the team, all coding conventions must live in version-controlled project files. Personal CLAUDE.md files provide individual preferences but should never contain team-critical rules."
  },
  {
    "domain": 2,
    "scenario": "[Scenario 2: Claude Code Team Setup] A project's root CLAUDE.md has grown to 500+ lines containing coding standards, an API reference, onboarding docs, and release procedures. Developers notice higher costs per session and Claude occasionally ignores rules buried deep in the file.",
    "question": "What is the recommended approach to restructure this?",
    "options": {
      "A": "Split the CLAUDE.md into one file per developer with their relevant sections",
      "B": "Keep CLAUDE.md short with only always-relevant rules. Move reference material to separate files that Claude reads on demand via @path imports, so bulky content is pulled in just-in-time rather than loaded every session",
      "C": "Move everything to .claude/rules/ files to reduce CLAUDE.md size",
      "D": "Increase the context window setting to accommodate the larger CLAUDE.md"
    },
    "answer": "B",
    "explanation": "CLAUDE.md is injected into every session, consuming context on each turn. A 500+ line CLAUDE.md wastes tokens, increases cost, and buries important rules where they get less attention (lost-in-the-middle effect). The fix is to keep CLAUDE.md lean with consistently-relevant guidance and move reference material to separate files pulled via @path imports on demand. .claude/rules/ is for path-scoped rules, not a general dumping ground for all content."
  },
  {
    "domain": 1,
    "scenario": "[Scenario 3: Multi-Agent Research System] A research coordinator delegates to three subagents: web_search, analysis, and synthesis. The synthesis subagent produces a report that contradicts the analysis subagent's findings. Logs confirm the analysis subagent returned correct, detailed results.",
    "question": "What is the most likely cause of the contradiction?",
    "options": {
      "A": "The synthesis model is too small and should be upgraded to Opus",
      "B": "The coordinator passed the raw user query to the synthesis subagent instead of passing the analysis subagent's output as the synthesis subagent's primary input — each subagent should receive precisely the inputs it needs from prior stages",
      "C": "The synthesis subagent should run in the same context as the analysis subagent",
      "D": "The coordinator should run synthesis multiple times and pick the best result"
    },
    "answer": "B",
    "explanation": "In the Multi-Agent Research System scenario, context passing between subagents is critical. The coordinator must orchestrate information flow: the synthesis subagent should receive the analysis output as its primary input, not just the raw user query. Passing only the raw query causes the synthesis agent to independently interpret the data, producing contradictions. The coordinator's job is to pack a self-contained briefing into each subagent spawn, with content separated from metadata so attribution survives handoff."
  },
  {
    "domain": 5,
    "scenario": "[Scenario 3: Multi-Agent Research System] A multi-agent research system synthesizes findings from four subagents into a final report. The coordinator passes all raw subagent outputs (totaling ~150K tokens) directly into the synthesis agent's context. The synthesis agent produces a shallow summary that misses key findings from the middle sections.",
    "question": "What two problems does this design create?",
    "options": {
      "A": "The synthesis agent needs extended thinking enabled and a larger model",
      "B": "Context bloat from raw tool outputs degrades reasoning quality, and the lost-in-the-middle effect causes findings in the middle of large inputs to receive less attention. The coordinator should trim outputs to relevant excerpts and place critical summaries at the beginning.",
      "C": "The subagents should produce shorter outputs to reduce total token count",
      "D": "The synthesis should be split into multiple sequential calls, one per subagent"
    },
    "answer": "B",
    "explanation": "Two compounding problems: (1) Context bloat — including 150K tokens of raw subagent output saturates the context window, degrading reasoning quality. The coordinator should trim each subagent's output to relevant excerpts. (2) Lost-in-the-middle — information placed in the middle of large inputs gets less attention. The coordinator should place the most critical summaries at the beginning of the aggregated input. Both are frequently tested anti-patterns in the Multi-Agent Research System scenario."
  },
  {
    "domain": 4,
    "scenario": "[Scenario 4: Developer Productivity Tools] A team builds an MCP server that helps developers navigate an unfamiliar codebase. The server exposes 15 tools including read_file, search_code, list_directory, get_file_tree, find_references, get_definition, get_imports, analyze_complexity, run_tests, format_code, lint_file, git_log, git_blame, git_diff, and open_in_editor. During testing, Claude frequently selects the wrong tool.",
    "question": "What is the primary cause and the best architectural fix?",
    "options": {
      "A": "Claude needs better system prompt instructions explaining each tool",
      "B": "15 tools exceeds the optimal range of 4-5 per agent. Split the tools across specialized agents (e.g., a navigation agent with read/search/list, a code analysis agent with references/definitions/complexity, and a git agent with log/blame/diff)",
      "C": "Rename the tools with numeric prefixes so Claude can select by number",
      "D": "Add a routing classifier that pre-selects the tool before Claude sees them"
    },
    "answer": "B",
    "explanation": "In the Developer Productivity Tools scenario, tool count directly impacts selection accuracy. Research shows 4-5 tools per agent is optimal; 15 tools significantly degrades selection. The fix is to split tools across specialized agents, each with a focused set: navigation (read, search, list, tree), analysis (references, definitions, complexity), and git operations (log, blame, diff). This also enables model-per-role optimization — navigation can use Haiku while analysis uses Sonnet."
  },
  {
    "domain": 4,
    "scenario": "[Scenario 4: Developer Productivity Tools] A developer productivity agent uses an MCP tool to search a large codebase. The search returns 500KB of JSON results containing every occurrence of a pattern across 200 files. This single tool result consumes most of the remaining context window.",
    "question": "How should this be handled to prevent context degradation?",
    "options": {
      "A": "Increase the context window to the maximum available size",
      "B": "Use a PostToolUse hook to truncate or summarize the oversized results before they enter the context, or delegate processing of the large result to a subagent with fresh context that returns only a compact summary",
      "C": "Set the tool to always return a maximum of 10 results",
      "D": "Skip the search and ask the developer to narrow their query"
    },
    "answer": "B",
    "explanation": "Large tool results can saturate the context window and degrade subsequent reasoning. Two complementary strategies are: (1) PostToolUse hooks that truncate or summarize oversized results before they enter context (context trimming), and (2) delegating processing of large results to a subagent with fresh context that returns only a compact summary to the coordinator. Hard-limiting results to 10 may miss important matches. The Developer Productivity scenario specifically tests context management with verbose codebase exploration outputs."
  },
  {
    "domain": 2,
    "scenario": "[Scenario 5: Claude Code in CI/CD] A DevOps team wants Claude Code to automatically review every PR in their GitHub Actions pipeline. The review should produce structured JSON output that a downstream step parses to post comments on the PR. The pipeline must be fully non-interactive with no user prompts.",
    "question": "Which combination of Claude Code features enables this?",
    "options": {
      "A": "Run Claude Code interactively with --auto-approve and capture terminal output",
      "B": "Use the -p flag with the review prompt for non-interactive execution, --output-format json for machine-parseable output, and each invocation runs in an isolated session by default",
      "C": "Use --batch-mode with --format=structured for CI-friendly output",
      "D": "Pipe the diff via stdin and parse Claude's natural language response with regex"
    },
    "answer": "B",
    "explanation": "The CI/CD scenario tests the canonical headless integration pattern: -p runs Claude Code non-interactively with a single prompt (natural session isolation since each invocation starts fresh), and --output-format json produces structured JSON output that downstream CI steps can parse programmatically. Interactive mode with --auto-approve is an anti-pattern for CI — it's not truly headless. Regex parsing of natural language output is brittle."
  },
  {
    "domain": 2,
    "scenario": "[Scenario 5: Claude Code in CI/CD] A CI pipeline runs Claude Code to review Apex code in a Salesforce project. The review frequently produces false positives — flagging code patterns that are actually acceptable per the team's Salesforce conventions. This wastes developer time triaging irrelevant findings.",
    "question": "How should false positives be reduced?",
    "options": {
      "A": "Lower Claude's temperature in CI mode to make it less creative",
      "B": "Add project-specific conventions and acceptable patterns to CLAUDE.md and .claude/rules/ files with glob patterns matching Apex files, so Claude Code has context about what is acceptable in this codebase",
      "C": "Filter the JSON output with a script that removes known false positive patterns",
      "D": "Switch to a smaller model that produces fewer findings overall"
    },
    "answer": "B",
    "explanation": "In the CI/CD scenario, false positives are reduced by giving Claude Code project-specific context. CLAUDE.md should contain the team's conventions (e.g., 'SOQL in loops is acceptable in batch Apex classes'), and .claude/rules/ files with glob patterns (e.g., **/*.cls) provide path-specific guidance. This is the idiomatic approach — Claude Code reads these files automatically and applies them contextually. Post-processing filters are brittle and miss new false positive patterns."
  },
  {
    "domain": 3,
    "scenario": "[Scenario 6: Structured Data Extraction] A pipeline extracts structured data from legal contracts. The JSON schema requires fields like arbitration_clause, penalty_amount, and expiration_date. Not every contract contains all fields. The current schema marks all fields as required, causing Claude to hallucinate values for missing fields.",
    "question": "What is the correct schema design fix?",
    "options": {
      "A": "Add a system prompt instruction saying 'leave fields blank if not found'",
      "B": "Make optional fields nullable in the JSON schema and instruct the model to return null when information is absent rather than guessing — this gives Claude a valid way to express 'not found' without fabricating data",
      "C": "Remove optional fields from the schema entirely so they're never extracted",
      "D": "Add a post-processing step that strips any values that look suspicious"
    },
    "answer": "B",
    "explanation": "In the Structured Data Extraction scenario, nullable fields are the key defense against hallucination. When all fields are required, Claude has no valid way to express 'not found' and resorts to fabrication to satisfy the schema constraint. Making fields nullable with explicit null values gives the model a legitimate output for absent data. This is distinct from syntax errors (which validation-retry loops fix) — it addresses semantic errors where the source data simply lacks the information."
  },
  {
    "domain": 3,
    "scenario": "[Scenario 6: Structured Data Extraction] A structured data extraction pipeline processes invoices from 15 vendor formats. The team uses a single prompt with a JSON schema and a validation-retry loop. Format compliance is 99%, but semantic accuracy varies wildly: 98% for standard invoices, 72% for handwritten ones, and 45% for multilingual invoices. The team reports aggregate accuracy of 91%.",
    "question": "What are the two reliability problems with this evaluation and pipeline design?",
    "options": {
      "A": "The validation-retry loop should be removed to reduce latency",
      "B": "Aggregate accuracy masks per-format failures (45% multilingual is unacceptable), and the pipeline lacks per-format routing — handwritten and multilingual invoices should be routed to specialized prompts with format-specific few-shot examples or escalated to human review",
      "C": "The team should increase temperature for multilingual invoices to explore more interpretations",
      "D": "All 15 formats should use separate JSON schemas to improve accuracy"
    },
    "answer": "B",
    "explanation": "Two problems: (1) Aggregate metrics masking per-category failures — 91% overall hides a 45% failure rate on multilingual invoices. Per-format accuracy must be tracked separately. (2) No per-format routing — difficult formats need specialized handling: format-specific few-shot examples, dedicated prompts, or human review for low-confidence extractions. The Structured Data Extraction scenario specifically tests understanding that validation-retry loops fix syntax errors but cannot fix semantic accuracy problems caused by insufficient format-specific guidance."
  },
  {
    "domain": 3,
    "scenario": "A developer is writing a system prompt for a customer support agent using the PRECISE framework (Persona, Role, Explicit criteria, Context, Instructions, Steps, Examples). The prompt includes a detailed Persona, clear Instructions, and step-by-step procedures, but lacks the Examples section. During testing, the agent's tone and response length vary wildly between interactions.",
    "question": "What is the most likely cause of the inconsistent tone and length?",
    "options": {
      "A": "The temperature setting is too high and should be reduced to 0",
      "B": "Without concrete Examples in the prompt, Claude lacks a calibration anchor for tone, length, and format — adding 2-3 few-shot examples of ideal responses would establish the expected output pattern",
      "C": "The Persona section is too detailed and is confusing the model",
      "D": "The system prompt should be moved to the user message for better adherence"
    },
    "answer": "B",
    "explanation": "The PRECISE framework emphasizes that Examples serve as calibration anchors. Without them, the model has instructions but no concrete reference point for what 'good' looks like in terms of tone, length, and format. Few-shot examples are one of the most effective ways to stabilize output consistency. This is a key prompt engineering concept tested in the CCA-F exam."
  },
  {
    "domain": 1,
    "scenario": "An architect is designing a system where inbound customer requests vary in complexity. Simple FAQ queries (80% of traffic) need fast, cheap responses, while complex account disputes (20%) require deep multi-step reasoning with tool access. The current design sends all requests to a single Claude Sonnet agent.",
    "question": "Which agentic pattern best optimizes this system for cost and quality?",
    "options": {
      "A": "Use a single Opus agent for all requests to maximize quality across the board",
      "B": "Implement a routing pattern: a lightweight classifier (Haiku) triages requests, routing simple queries to Haiku with canned responses and complex disputes to Sonnet or Opus with full tool access",
      "C": "Process all requests with Haiku to minimize cost, then retry failed ones with Opus",
      "D": "Use prompt chaining: every request goes through classify → research → respond stages regardless of complexity"
    },
    "answer": "B",
    "explanation": "The routing pattern is ideal when request complexity varies significantly. A lightweight classifier (Haiku) can quickly categorize requests at minimal cost, then route them to the appropriate model tier. Simple queries go to Haiku for speed and cost efficiency, while complex disputes are routed to more capable models (Sonnet/Opus) with tool access. This avoids the waste of running all requests through expensive models or the quality loss of running everything through cheap ones."
  },
  {
    "domain": 4,
    "scenario": "A team is building an MCP server that provides Claude with access to a production PostgreSQL database containing customer PII (names, emails, payment info). The developer proposes exposing a generic execute_sql tool that accepts arbitrary SQL queries so Claude can flexibly answer any data question.",
    "question": "What is the primary architectural concern with this approach?",
    "options": {
      "A": "SQL queries are too slow for real-time agent interactions",
      "B": "MCP servers cannot connect to databases directly",
      "C": "Exposing arbitrary SQL execution violates least-privilege principles — the server should expose purpose-specific tools (e.g., lookup_customer, get_order_status) with parameterized queries that limit access to only the data and operations the agent needs",
      "D": "The tool description will be too long for Claude to understand"
    },
    "answer": "C",
    "explanation": "Least-privilege tool scoping is a core MCP security principle. A generic execute_sql tool gives the agent unrestricted access to the entire database, including destructive operations (DROP TABLE, DELETE) and sensitive data it doesn't need. Purpose-specific tools with parameterized queries enforce boundaries: the agent can only perform pre-approved operations on pre-approved data. This is a key security architecture concept tested in the CCA-F exam."
  },
  {
    "domain": 5,
    "scenario": "A knowledge assistant needs to answer questions using a company's internal documentation totaling 50,000+ pages that update weekly. A developer proposes loading all documents into Claude's 200K context window for each query to ensure comprehensive answers.",
    "question": "Why is this approach impractical, and what is the recommended alternative?",
    "options": {
      "A": "The context window is large enough; the developer should proceed with loading all documents",
      "B": "50,000+ pages far exceed the context window capacity. The recommended approach is RAG (Retrieval-Augmented Generation): index documents in a vector database, retrieve only relevant chunks per query, and pass those chunks as context to Claude",
      "C": "The developer should split the documents across 250 separate API calls and merge the answers",
      "D": "Claude should be fine-tuned on the documentation instead of using context"
    },
    "answer": "B",
    "explanation": "50,000+ pages (~12.5M+ tokens) vastly exceed Claude's 200K context window. RAG is the standard architecture for large knowledge bases: documents are chunked, embedded, and stored in a vector database. At query time, only the most relevant chunks are retrieved and injected into the context window. This gives Claude access to relevant information without exceeding context limits, and the weekly updates only require re-indexing changed documents."
  },
  {
    "domain": 2,
    "scenario": "A developer wants to use Claude Code across three environments: the CLI terminal, VS Code with the Claude extension, and a CI/CD pipeline. They want consistent project context (coding standards, architecture decisions) available in all three environments.",
    "question": "What is the best way to ensure consistent project context across all Claude Code environments?",
    "options": {
      "A": "Create separate configuration files for each environment",
      "B": "Use the project-level CLAUDE.md file checked into the repository — it is automatically loaded by Claude Code regardless of whether it runs in the CLI, an IDE extension, or CI/CD, providing consistent project context across all environments",
      "C": "Store all context in the user-level ~/.claude/CLAUDE.md file",
      "D": "Pass the context as a command-line flag each time Claude Code is invoked"
    },
    "answer": "B",
    "explanation": "The project-level CLAUDE.md is the canonical source of persistent project context. Because it lives in the repository, it is version-controlled and automatically loaded by Claude Code in any environment — CLI, IDE extensions, or headless CI/CD. This ensures every developer and every pipeline run operates with the same coding standards and architectural decisions. User-level CLAUDE.md provides personal defaults but is not shared across the team."
  },
  {
    "domain": 1,
    "scenario": "An architect is choosing between an evaluator-optimizer pattern and an orchestrator-worker pattern for a content generation pipeline. The pipeline needs to produce marketing copy that meets strict brand guidelines. Initial drafts often need multiple rounds of revision before they comply.",
    "question": "Which pattern is the better fit and why?",
    "options": {
      "A": "Orchestrator-worker, because it allows parallel generation of multiple copy variants",
      "B": "Evaluator-optimizer: one agent generates the copy, a separate evaluator agent checks it against brand guidelines, and if it fails, the generator revises based on the evaluator's feedback — this loop continues until the copy passes or a max iteration limit is reached",
      "C": "A single mega-prompt that includes all brand guidelines will produce compliant copy in one pass",
      "D": "Fan-out pattern with five generators and a majority-vote selector"
    },
    "answer": "B",
    "explanation": "The evaluator-optimizer pattern is designed for iterative refinement tasks. A generator produces output, an evaluator assesses it against defined criteria (brand guidelines), and feedback drives revision cycles. This is superior to orchestrator-worker here because the task isn't decomposable into independent subtasks — it's a single output that needs quality iteration. The pattern naturally supports a max-iteration guard to prevent infinite loops."
  },
  {
    "domain": 4,
    "scenario": "A team is migrating their MCP server from SSE transport to the newer recommended protocol. The server is deployed behind a cloud load balancer and serves multiple concurrent Claude Code clients. They need to maintain per-client session state across requests.",
    "question": "What is the recommended transport and how should session state be handled?",
    "options": {
      "A": "Keep using SSE since it is still the standard for remote MCP servers",
      "B": "Switch to Streamable HTTP (the recommended remote transport as of MCP 2025-11). Use the Mcp-Session-Id header to maintain per-client session affinity, and store session state in an external store (e.g., Redis) so any backend instance behind the load balancer can serve any client",
      "C": "Use stdio transport with SSH tunnels for each remote client",
      "D": "Use WebSocket transport with sticky sessions on the load balancer"
    },
    "answer": "B",
    "explanation": "Streamable HTTP replaced SSE as the recommended remote MCP transport. It uses the Mcp-Session-Id header for session management, which is critical in load-balanced deployments. Session state should be externalized (e.g., to Redis) rather than stored in-memory, so requests can be served by any backend instance. This is a key MCP transport evolution topic tested on the CCA-F exam."
  },
  {
    "domain": 1,
    "scenario": "A developer is implementing a multi-step document processing pipeline using the Claude Agent SDK. The pipeline has three stages: extract entities, classify document, and generate summary. Each stage's output feeds into the next. The developer considers running all three stages as parallel subagents to reduce latency.",
    "question": "Why is parallelization inappropriate here, and what pattern should be used instead?",
    "options": {
      "A": "Parallelization is fine — subagents can share state implicitly through the coordinator",
      "B": "The stages have sequential dependencies (classify needs extracted entities, summarize needs the classification), so they cannot run in parallel. Use prompt chaining: execute each stage as a separate API call, passing the output of one as the input to the next",
      "C": "All three stages should be combined into a single mega-prompt for simplicity",
      "D": "Use the fan-out pattern with each stage processing the original document independently"
    },
    "answer": "B",
    "explanation": "Prompt chaining is the correct pattern when stages have sequential dependencies — each step's output is required as input for the next. Parallelization only works when tasks are independent. Here, classification depends on extracted entities, and summarization depends on the classification. Prompt chaining gives each stage a focused prompt, better error handling at each step, and clearer debugging. A mega-prompt risks lower quality due to task complexity competing for attention."
  },
  {
    "domain": 2,
    "scenario": "A team wants to add a PostToolUse hook in Claude Code that automatically runs 'prettier --write' on any file after Claude edits it. They also want a PreToolUse hook that blocks any Bash commands containing 'rm -rf'. The team is unsure how hooks interact with Claude's workflow.",
    "question": "How do Claude Code hooks work in this scenario?",
    "options": {
      "A": "Hooks are just instructions in CLAUDE.md that Claude follows voluntarily",
      "B": "PreToolUse hooks run before a tool executes and can block it (exit code non-zero blocks the action); PostToolUse hooks run after a tool completes and can modify output or trigger side effects. Both are deterministic shell commands — they enforce behavior programmatically, not through prompt instructions",
      "C": "Hooks can only log events; they cannot block or modify tool behavior",
      "D": "PostToolUse hooks run before the tool, and PreToolUse hooks run after — the naming is counterintuitive"
    },
    "answer": "B",
    "explanation": "Claude Code hooks are shell commands triggered by tool lifecycle events. PreToolUse hooks intercept tool calls before execution — a non-zero exit code blocks the tool call entirely, making them ideal for security guardrails (blocking dangerous commands). PostToolUse hooks run after tool completion and can trigger automated follow-up actions like formatting. Both are deterministic and programmatic, unlike CLAUDE.md instructions which are advisory."
  },
  {
    "domain": 3,
    "scenario": "A developer wants Claude to output a chain-of-thought reasoning process for a classification task but also needs the final answer in a machine-parseable format. Using extended thinking would work but adds significant token cost. The developer wants a cheaper alternative.",
    "question": "What prompt engineering technique achieves visible reasoning with structured output without using extended thinking?",
    "options": {
      "A": "Set temperature to 0 to force deterministic output",
      "B": "Use XML tags to structure the response: instruct Claude to write its reasoning inside <reasoning> tags first, then output the final classification inside <answer> tags. The downstream parser extracts only the <answer> block while the reasoning remains available for debugging",
      "C": "Ask Claude to think step by step and hope the final answer appears last",
      "D": "Use two separate API calls: one for reasoning and one for the answer"
    },
    "answer": "B",
    "explanation": "XML tags provide a structured way to separate reasoning from final output within a single response. By instructing Claude to use <reasoning>...</reasoning> for its thought process and <answer>...</answer> for the parseable result, you get both human-readable reasoning and machine-parseable output. This is cheaper than extended thinking (reasoning tokens count toward output but avoid the thinking token overhead) and more reliable than hoping for consistent formatting."
  }
]
